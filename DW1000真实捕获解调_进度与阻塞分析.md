# DW1000 真实捕获解调：进度与阻塞情况说明

> 更新日期：2026-08-11
> 结论口径：本轮已修复并验证多处 MATLAB/C++ 实现差异，但 X410 真实捕获尚无帧通过 FCS，因此不能认定真实信号解调已经完成。

## 1. 目标和实测配置

目标是在 `gr-uwb` 的 `UwbRealtimeDemodulator` 中处理 DW1000 信号，完成前导、SFD、PHR、PSDU 和 FCS 解码，并保持已有 QM35825/code-9 回归不退化。

本轮 X410 配置如下：

| 项目 | 配置 |
|---|---|
| 中心频率 | **6489.6 MHz** |
| X410 原始采样率 | 737.28 MS/s |
| 解调域采样率 | 经 65/48 重采样至 998.4 MS/s |
| 接收增益 | 60 dB |
| 天线 | TX/RX0 |
| DW1000 preamble code | 10 |
| Preamble repetitions | 256 |
| SFD | Decawave 8-symbol SFD |
| 数据率（当前 PHR 判定） | 6.81 Mb/s |

注意：此前实验中出现过 6500 MHz 加数字频移的配置，但这不适用于当前信号。本轮最终实测全部使用 6489.6 MHz 直接调谐。

## 2. 本轮确认并修复的问题

### 2.1 Code-10 循环原点

`kPreambleCode10` 原循环位置与 MATLAB `HRPCodes(10)` 生成的测试波形不一致，导致理想码片模板相关和后续 NS-SFD 解调恶化。

当前实现已按 `testdata/uwb_code10_preamble16_payload8.cfile` 校准循环原点，并增加相关系数回归检查。正确原点与 MATLAB 波形的归一化相关约为 0.999。

### 2.2 长前导的 CIR 估计区间

MATLAB `estimateCirAndSoftChips` 使用前导末尾、紧邻 SFD 的若干 SYNC 重复估计 CIR。旧 C++ 逻辑沿用了 64-SYNC 配置的 `[skip, skip+count)` 区间；对于 256-SYNC 前导，它错误地从前导前部取样。

当前实现改为使用最后 `cir_repetitions` 个可用 SYNC，并加入 256-SYNC 前后信道延迟不同的合成测试。该测试可区分旧实现和修复后的实现。

### 2.3 SFD 精同步结果未回写

MATLAB 在 NS-SFD 精同步后会重新计算并回写 preamble start。旧 C++ 虽然找到 SFD，却继续使用旧 preamble start 进入 CIR/soft-chip 阶段。

当前实现按照检测到的 SFD 起点和实测 symbol period 回写 preamble start，并在既有 SFD 偏移测试中锁定这一不变量。

### 2.4 分数 soft-chip 采样网格

MATLAB 使用 `measured_period / chips_per_symbol` 的分数位置，并对 CIR 滤波输出线性插值。旧 C++ 将 samples-per-chip 四舍五入为整数 2，真实 SFO 会使误差随 payload 持续累积。

当前实现保留精确 2 samples/chip 的优化路径；存在 SFO 时改用分数位置和线性插值。新增的 `period=1016.25` 合成测试在旧实现下 NS-SFD metric 低于门限，修复后通过。

### 2.5 Payload 解码顺序

当前实现优先执行与 MATLAB 对应的固定 PHR 长度、NS-SFD 极性、硬 BPM 和硬 Viterbi 路径。只有该路径未通过 FCS 时，才执行长度邻域、反极性和软 Viterbi 等实验性回退，避免较短的偶然 RS 成功覆盖固定长度结果。

此外，保留了此前针对真实 RF 的 code-10 参数透传、PHR SECDED 长度能量守卫、软 Viterbi及回退路径。这些措施提高了进入 `fcs_failed` 阶段的帧数，但尚未产生真实 FCS 成功帧。

## 3. 调度与实时实现说明

`UwbRealtimeDemodulator` 是消息/PDU block，不在流式 `work()` 中执行整帧解调。输入 PDU 进入有界任务队列，由固定 worker 线程处理并发布结果；因此解调过程不会改变 GNU Radio 流式 scheduler 的 consume/produce 语义。

本轮没有在实时处理循环中引入逐包无界内存增长。已有 scratch buffer 继续由 worker 复用；分数网格仅在存在 SFO 时进入标量插值分支，精确整数网格仍走原优化路径。

## 4. 测试结果

### 4.1 构建和 QA

- `cmake --build . -j2`：成功。
- `ctest -R 'uwb_qa_uwb_demod_core|uwb_qa_uwb_realtime_demodulator'`：2/2 通过。
- 全部 12 项 CTest：11 项通过。
- 唯一失败项为既有 `uwb_qa_uwb_pdu_rational_resampler` 吞吐门限：约 376–378 PDU/s，低于测试要求的 400 PDU/s；其端到端功能和 FCS 检查通过。该性能失败与本轮 demod core 修改无直接关系。

新增或增强的主要 QA：

1. MATLAB code-10 波形循环原点相关检查；
2. 256-SYNC 前导必须使用末尾 SYNC 估计 CIR；
3. `period=1016.25` 时分数 soft-chip 网格能够通过 NS-SFD；
4. SFD 精同步必须回写 preamble start；
5. 原 code-9/QM35825 合成 golden 保持通过。

### 4.2 已有真实捕获回放

对 `/tmp/uwb-hwtest/dw_frames` 中 frame 1 至 frame 289 回放：

- 0 帧 `success`；
- 0 帧 `fcs_pass=true`；
- 绝大多数帧到达 `payload_failed` 或 `fcs_failed`；
- 分数采样修复使部分帧由 `payload_failed` 前进到 `fcs_failed`，说明修复有效，但不足以完成最终解调。

长度邻域回退会出现不同长度的偶然 RS 成功，因此“RS 通过数量”不能作为验收依据，必须以固定 PHR 长度路径和最终 FCS 为准。

### 4.3 X410 6489.6 MHz 实测

X410 可正常发现，设备信息为 X410、FPGA `C1_600`。在 6489.6 MHz、60 dB、TX/RX0 上进行了两类采集。

实时检测/截帧结果：

- detector 候选：134；
- resampler 接收/输出：134/134，丢弃 0；
- 写出帧：26，丢弃 0；
- 解码 frame 1 至 25：`sfd_failed=1`、`fcs_failed=7`、`payload_failed=17`；
- `success=0`，`fcs_pass=0`。

原始采样结果：

- 737.28 MS/s，约 220 万复采样点；
- 检测到 6 组稳定前导，每组具有 256 个 SYNC 和 6 个非零 Decawave SFD 符号的相关结构；
- 同步窄带分量幅度约 `2.96e-7`，输入 RMS 约 `3.69e-2`；抵消该分量前后解调结果基本相同，因此它不是当前主阻塞。

扩大 CIR 平均重复数的临时实验也没有产生 FCS 成功，相关实验修改未保留在正式代码中。

## 5. 当前结论

已经确认并修复的三处 MATLAB/C++ 关键差异——长前导 CIR 区间、SFD 时序回写、分数 soft-chip 网格——均有能够在旧实现失败、在新实现通过的测试例。

这些修复提升了真实信号的中间阶段表现，但 **已有捕获和本轮 6489.6 MHz X410 新捕获仍然没有任何 FCS 成功帧**。当前问题已经从检测、前导和 NS-SFD 阶段收敛到 PHR 后的 BPM、卷积解码、RS 或 payload 定界路径。

当前不能仅根据重复出现的 payload 前缀或偶然 RS 成功认定数据正确；FCS 全部失败说明至少仍有一个逐比特实现差异或输入真值/PHY 配置尚未对齐。

## 6. 下一步所需基准

继续定位需要以下任意一种信息：

1. MATLAB 对同一个 X410 捕获帧的逐阶段输出：preamble start、SFD start/end、CIR/first path、soft chips、PHR bits、BPM hard bits、Viterbi bits、RS 输出和最终 PSDU；或
2. 发射端准确 PHY 配置及已知 PSDU 字节。

建议优先选取当前捕获中的一个 `fcs_failed` 帧，在 MATLAB 和 C++ 中固定相同的 preamble start、code 10、256-SYNC 和 Decawave SFD，逐阶段寻找第一处分歧。当前环境没有可执行 MATLAB，也没有这些 code10/256 帧对应的 MATLAB golden 中间数据，因而暂时无法将阻塞进一步归因到 BPM、Viterbi 还是 RS/FCS。

## 7. 关键源码

| 内容 | 文件 |
|---|---|
| PHY profile、code-10 和 SFD 表 | `gr-uwb/include/gnuradio/uwb/uwb_phy_profile.h` |
| 完整解调阶段 | `gr-uwb/include/gnuradio/uwb/uwb_demod_core.h` |
| Block 参数和 PDU 发布 | `gr-uwb/include/gnuradio/uwb/uwb_realtime_demodulator.h`、`gr-uwb/lib/uwb_realtime_demodulator.cc` |
| Python bindings | `gr-uwb/python/uwb/bindings/python_bindings.cc` |
| QA | `gr-uwb/lib/qa_uwb_demod_core.cc` |
| MATLAB 参考实现 | `UWB_demodulation/` |
| MATLAB 生成测试信号 | `testdata/uwb_code10_preamble16_payload8.cfile` |

## 8. 验收状态

| 验收项 | 状态 |
|---|---|
| 修复项具有合成回归测试 | 通过 |
| Demod core/realtime QA | 通过 |
| X410 正确频率完成采集 | 通过 |
| 实时检测、重采样和截帧无丢包 | 通过 |
| 真实 DW1000 PSDU FCS | **未通过** |
| MATLAB 同帧逐阶段一致性 | **待提供/待执行** |

综上，本轮交付状态是：**实现差异修复和验证基础已完成；真实 DW1000 完整解调仍处于阻塞状态。**
