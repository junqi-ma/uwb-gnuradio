# UWB GNU Radio 当前开发状态

> 更新日期：2026-08-19
>
> 本文是项目进度的唯一主线入口。历史长版状态已归档到
> [`docs/archive/开发状态_2026-08-09历史快照.md`](docs/archive/开发状态_2026-08-09历史快照.md)。

## 0. 2026-08-14 最新验证结论

### 0.1 QM35 盲 t0 自动锁定（mixed 737.28，离线）

`main` `af97089` 已包含 `UwbAutoScheduledExtractorSc16`：未知 `t0`、已知 T=5 ms
时，从原生 SC16 @737.28 MS/s 盲捕获第一个可确认 QM35，再切到 scheduled 截窗。
能量门只在 `ACQUIRE/REACQUIRE` 运行；进入 `LOCKED` 后必须关闭全局 Region。

本轮在真实 mixed 文件上用**完整解调反馈**（不是 loopback）验证：

```text
file_source SC16 @737.28
  -> UwbAutoScheduledExtractorSc16   # 不提供 first_packet_sample
  -> PDU 65/48 quality_minorder
  -> UwbRealtimeDemodulator (code 9, 64 SYNC, 4z2, bypass)
  -> schedule_feedback -> lock_obs
```

输入：`/mnt/f/UWB基带数据/dw1000_qm35_mixed_6489p6MHz_737p28Msps_0p5s_sc16_20260811_01.dat`
（0.5 s，368,640,000 SC16 samples）。脚本：
[`testdata/offline_qm35_auto_lock.py`](testdata/offline_qm35_auto_lock.py)。

| 项目 | 结果 |
|---|---|
| 用户 t0 | **未提供** |
| 首个确认包 | acquisition `start=1690126`（约 2.292 ms），FCS pass |
| 身份确认墙钟 | 0.10 s |
| 终态 | `locked`，`identity_confirmed=true` |
| 锁定 T | 0.005000034 s（3,686,425 native samples，约 +6.8 ppm） |
| 能量 Region | 捕获期 5，**锁定后 0** |
| 窗口 | 1 acquisition + 99 scheduled，**0 drop** |
| 解调 | 100 收、99 success、**99/100 FCS**、1 个 `payload_failed` |

这证明离线 mixed 流上“盲 t0 + 已知 5 ms”的最小闭环已经打通。方案
§18 的 X410 随机启动、60 s / 10 min soak、overflow 重捕获、MATLAB 逐样本对照
以及本机 `qa_uwb_auto_scheduled_extractor_sc16` 全绿 **尚未完成**，不能写成
硬件盲捕获验收通过。

锁定后的原生 SC16 落盘（头 300 µs、尾 100 µs）已在同一 mixed 文件上打通：
`--write-sc16` 写出 `capture.iq` + `capture.jsonl`，0.5 s **100/100 与源文件
逐样本一致、0 drop**。落盘后用
`testdata/decode_scheduled_sc16_dump.py`（GR）或
`testdata/decode_scheduled_sc16_dump.m`（MATLAB `decode_uwb`）按窗解析并解调。
解调默认窗仍是 10/190/4.1 µs；dump 时才改用
`kNativeInterferencePreGuard=221184`、`PostGuard=73728`。尾短于头，因为
QM35 CIR 取自前导，不必再为只压 payload 的迟到 DW1000 留整包。详见
[`docs/phase1/下一步_锁定后SC16截取与DW1000头尾预留.md`](docs/phase1/下一步_锁定后SC16截取与DW1000头尾预留.md)。

详细过程与复现见
[`docs/phase1/测试报告_QM35盲捕获与自动周期锁定_mixed737p28.md`](docs/phase1/测试报告_QM35盲捕获与自动周期锁定_mixed737p28.md)。

### 0.1b 同一流程：无干扰 QM35（737.28，离线）

同一脚本、不提供 t0，输入换成
`/mnt/f/UWB基带数据/qm35_6489p6MHz_737p28Msps_0p5s_sc16_20260811_01.dat`
（metadata `scenario=QM35`，无 DW1000）。

| 项目 | 结果 |
|---|---|
| 用户 t0 | **未提供** |
| 首个确认包 | acquisition `start=3543362`（约 4.804 ms），FCS pass |
| 身份确认墙钟 | 0.20 s |
| 终态 | `locked`，`identity_confirmed=true` |
| 锁定 T | 0.005000069 s（3,686,451 native samples，约 +13.8 ppm） |
| 能量 Region | 捕获期 1，**锁定后 0** |
| 窗口 | 1 acquisition + 98 scheduled/provisional 写出，**1 drop**（末窗超出 0.5 s） |
| 与源文件 bit-exact | **99/99**（消单音前） |
| live / dump 解调 | **98/99 FCS**，id=24 `payload_failed` |
| 去单音 | RF **6256.640 MHz**（与 mixed 同一 CW），覆盖 `capture.iq`，不留 raw |

落盘目录：`F:\UWB基带数据\qm35_clean_scheduled_sc16_dump\`。C++ 真值：
`F:\USRP数据解调\decoded_results\scheduled_sc16_dump_qm35_clean\scheduled_dump_cpp.csv`。
MATLAB 入口：`run_decode_scheduled_sc16_dump.m`，把 `dumpDir` 换成该目录。

详见
[`docs/phase1/测试报告_QM35盲捕获与自动周期锁定_clean737p28.md`](docs/phase1/测试报告_QM35盲捕获与自动周期锁定_clean737p28.md)。

## 0.5 2026-08-12 验证结论（仍有效）

### 0.2 实时解调器配置与正确性

- `UwbRealtimeDemodulator` 的 HRP BPRF preamble code 已支持 **9、10、11、12**；
  GRC 中改为受限可选项，默认仍为 QM35 的 code 9。
- MATLAB `lrwpan.internal.HRPCodes(9..12)` 已作为代码表和 QA 基准导入；code 11/12
  的 BPRF scrambler 初始状态也已与 MATLAB 对齐。
- `cir_filter_mode` 的默认值已改为 **`bypass`**。这会绕过软 chip CIR 匹配 FIR；
  仍可显式选择 `auto`、`full` 或 `rake` 做算法对照。
- CFO 先做 bootstrap SFD 定位，再从 SFD 反推 preamble origin 重追踪 SYNC；丢弃前
  24 个 SYNC 后做相位线性拟合，与 MATLAB `compensateCarrierOffset` 对齐。
- SFD 先测试 timing 预测位置，失败后每次回溯一个 SYNC、快速路径最多 10 次。
  修复了“仅在峰数恰好等于配置 SYNC 数才回溯”的问题：实际 DW1000 出现 127/128
  个峰时也可回溯并锁定 SFD。异常深度 timing 偏差仍保留旧的 32-window fallback。

真实 998.4 MS/s `dw1000_new_processed_1.dat` 的 MATLAB / GNU Radio 对照：

| 项目 | MATLAB | GNU Radio |
|---|---:|---:|
| 配置 | code 11、128 SYNC、DW-8 | 相同，CIR bypass |
| CFO | -2.566 kHz | -2.673 kHz |
| SFD / NS-SFD | DW-8，相关 0.9818 | SFD 0.742，NS-SFD 0.986 |
| PSDU | 12 B | 12 B，相同内容 |
| FCS | pass (`0x4440`) | pass |

GNU Radio 的 bootstrap SFD 在该包回溯 **6 个 SYNC** 后成功；preamble 与 SFD
绝对位置均与 MATLAB 相差不超过 1 个 998.4 MS/s 样点。

### 0.3 737.28 MS/s DW1000 干扰捕获实测

输入：`/mnt/f/UWB基带数据/dw1000_interference_6489p6MHz_737p28Msps_0p5s_sc16_20260811_01.dat`。
该文件为 0.5 s、368,640,000 个 SC16 complex samples；能量包络给出约 **974** 个周期
突发，间隔约 **513.333 us**（约 1948 pkt/s）。实测配置：

```text
SC16 @737.28 MS/s
  -> UwbScheduledExtractorSc16 (t0=40636, T=378470.4 samples)
  -> PDU 65/48 (quality_minorder, SC16->FC32 only inside each window)
  -> UwbRealtimeDemodulator (code 10, 256 SYNC, Decawave DW-8, bypass)
```

全文件结果：974/974 scheduled windows 进入 resampler 和 demod；demod **974 complete、
0 failed、0 dropped**。已收集的 973 条结果均为 success + FCS pass；末尾少 1 条仅是
EOS 时 `message_debug` 发布时序，完成计数并未失败。

| 口径（2 demod workers） | 结果 |
|---|---:|
| 所有窗口完成（不含尾部 debug 排空） | 11.68 s，83.4 pkt/s |
| 含尾部排空墙钟 | 12.38 s，78.6 pkt/s |
| 单 worker 所有窗口完成 | 11.63 s，83.8 pkt/s |
| 单包 demod 均值 / P95 | 5.386 / 6.479 ms |
| PDU 65/48 FIR 均值 | 2.79 ms/包 |
| PDU handler 均值（含转换、PMT、发布） | 3.20 ms/包 |

单 worker 与双 worker 端到端几乎相同，说明当前离线全文件吞吐由连续 SC16 文件读取、
大 PDU/PMT 传递和 PDU 65/48 供给限制，而不是 demod worker 算力限制。该捕获的
1948 pkt/s 发包率远高于本机约 83 pkt/s 离线处理率；不能以当前 host-only 路径宣称
此类密集 DW1000 流可实时处理。

973 个成功包的 demod worker 内部时间占比：

| 阶段 | 均值 | 占 demod 总时间 |
|---|---:|---:|
| timing（粗搜 + 细跟踪） | 2.556 ms | 47.5% |
| SFD | 1.827 ms | 33.9% |
| CIR bypass | 0.488 ms | 9.1% |
| CFO | 0.433 ms | 8.0% |
| NS-SFD / PHR / payload | 0.082 ms | 1.5% |

其中 timing 粗搜为 1.049 ms、细跟踪为 1.507 ms。当前链路使用已知周期截窗，
**未运行能量门或 detector coarse**；不得将 scheduled extraction 计作 packet detection
算法耗时。

## 1. 双线结论

项目当前分为两条隔离开发线。下表记录已提交的历史分支基线；本工作区其后仍有
2026-08-12 的解调、SC16 与 DW1000 验证改动，当前状态以第 0 节为准，不能据此表
推断工作区干净或所有改动已推送：

| 主线 | 分支 / HEAD | 当前结果 | 下一目标 |
|---|---|---|---|
| 实时解调性能 | `main` / `57c213a` | 300 pkt/s 稳定通过；新增 Full、Top-K RAKE、soft-chip Bypass 三条显式路径 | 独立测量并优化 PMT 元数据、消息发布、统计和调度开销 |
| Phase-2 实时 SIC | `feature/sic-validation` / `d265730` | S1/S2 完成：生产 DW1000 profile、复数 CIR、TX 重构和真实单包 cancellation golden | S3 slot core：direct-first、无 DW 快路、最多 4 个 DW、QM35 fallback、post-SIC CIR-only |

两条分支从 `5ce16e3` 后分别演进。SIC 分支尚未吸收 `main` 后续性能提交；在
S3 接口稳定前保持 worktree 隔离，不在两个目录间复制整份 C++ 文件。集成时应以
小提交 merge/rebase，并重新执行全部 QA 和真实 golden 对照。

## 2. 性能主线状态

权威分支：`main`，工作目录：`/home/junqima/workspace/uwb-gnuradio`。

最新提交链的关键成果：

- `eaf9ad3`：优化 timing 和 sparse RAKE。
- `23875c8`：增加 RAKE 鲁棒性 benchmark。
- `1e6990c`：隔离 demod core 与消息层开销。
- `82a6e23`：消除 realtime demod 输入复制。
- `6f30b65`：增加冷缓存分阶段测量，确认冷缓存不是约 3 倍差距的主因。
- `b39aa42`：避免无消费者时写回完整 complex CIR，每包减少约 1.2 MB 写流量。
- `57c213a`：增加 soft-chip Bypass 路径。

当前实测基线：

- Bypass 单 worker 约 **751 pkt/s**，2 worker 异步约 **1324 pkt/s**。
- 30 秒、300 pkt/s：**9000/9000 success，0 fail，0 drop**。
- Top-4 核心 CIR 七轮中位约 **433 us**；双 worker 峰值约 **784 pkt/s**。
- 删除 complex 写回后峰值约 782 → 784 pkt/s，吞吐提升不可区分，但内存写流量
  明确下降，因此作为低风险带宽优化保留。
- 当前构建的全部 **13 项 CTest** 通过；AWGN、CFO、多径与已有碰撞扰动测试通过。

当前判断：继续微调冷缓存 CIR 或小 helper 内联的收益很低。下一轮必须先把
PMT 字典构造、symbol intern、结果发布、统计原子操作、队列和 GNU Radio 调度分别
计时，再依据占比优化；不得用总吞吐噪声证明某个局部优化有效。

详细过程与复现实验见：

- [`Codex_UWB解调器性能优化记录.md`](Codex_UWB解调器性能优化记录.md)
- [`docs/performance/性能分析_解调分阶段耗时报告.md`](docs/performance/性能分析_解调分阶段耗时报告.md)
- [`docs/performance/性能分析测试报告_buffer与分层吞吐.md`](docs/performance/性能分析测试报告_buffer与分层吞吐.md)

## 3. SIC 主线状态

权威分支：`feature/sic-validation`，工作目录：
`/home/junqima/workspace/uwb-gnuradio-sic`。

已完成：

- 生产 `Dw1000Profile`：code 11、128 SYNC、Decawave SFD。
- 完整复数 CIR 接口，并兼容旧实部诊断字段。
- 真实 998.4 MHz DW1000 golden：timing、CFO、12-byte PSDU、FCS `0x4440`、
  complex CIR、TX impulse、replica 和 trial residual。
- `uwb_sic_core.h`：整数对齐、逐 SYNC CFO、全局复增益、trial/commit、安全门
  和失败逐样本旁路。
- `uwb_tx_reconstructor.h`：PHR/PSDU 校验、CRC/FCS、RS(63,55)、CL-3、
  BPRF scrambler/跳时、BPM-BPSK、preamble/SFD 和 complex-CIR 卷积。
- C++ TX impulse 与 MATLAB **178112 样本完全一致**；replica 相对 L2
  `5.19e-8`。
- 完整 C++ `decode → CIR → reconstruct → cancel` 抑制度 **16.2894 dB**；
  residual 相对 MATLAB L2 误差 **1.19068%**。
- 全部 **9 项 CTest** 与 `git diff --check` 通过。

S1/S2 已完成，但生产实时 SIC 尚未完成。仍需：

1. S3 自适应 slot core：无 DW 快路、direct-first、最多 4 个 FCS-valid DW、
   QM35 fallback 和 post-SIC CIR-only。
2. S4 `UwbRealtimeSic` message/PDU block：有界队列、worker scratch 复用、raw
   独立旁路、queue-full/异常恢复、stop/drain 和统计。
3. mixed-signal post-SIC CIR golden。
4. X410 前端同步单音去除、10 MHz 数字频移及逐样本输入契约闭环。
5. 100/200 slot/s 十分钟 soak、250 slot/s 容量和 200 slot/s p99 <20 ms。

SIC 方案详见 feature 分支根目录的 `开发方案_GNURadio实时SIC.md`。合并到 `main`
前不得把“单包 core 完成”表述为“实时 SIC block 完成”。

## 3.5 软件升采样 65/48 开发线（进行中）

任务书：[`开发方案_GNURadio软件升采样_737.28M到998.4M.md`](开发方案_GNURadio软件升采样_737.28M到998.4M.md)

- **Phase 0（调查）完成**：GNU Radio 3.10.1.1 / VOLK 2.5.1；`rational_resampler_ccf`
  为 `gr::block`+`forecast/general_work`，taps 按 `i%65` 拆相、`history=ceil(len/65)`，
  默认 **TPP_ALL**，内核用 `volk_32fc_32f_dot_prod_32fc_a`；内置块边界语义为前向
  非因果（首输出即稳态、有限长输出减约 2×群时延），与 upfirdn 边界约定不同。
- **Phase 1（滤波器+golden）完成**：quality（B=330MHz，5363 taps，阻带 −97 dB）
  与 realtime（B=290MHz，2635 taps，阻带 −96 dB）双档设计；7 组 golden
  （impulse/dc/tone×3/random/uwb）经 scipy upfirdn 导出并逐样本验证；
  契约 = upfirdn（`len_out=ceil(((N−1)·65+T)/48)`，零填充两侧全卷积）。
  详见 [`docs/performance/测试报告_GNURadio软件升采样_65_48.md`](docs/performance/测试报告_GNURadio软件升采样_65_48.md)。
- **Level A benchmark（Grok Build 完成）**：内置 `rational_resampler_ccf`
  三档实测——default 67.2、quality 44.4、realtime 49.3 input MS/s，最优仅
  **9% 实时率**（source_only 基线 1359 MS/s 证明瓶颈在 resampler 算力）。
  **Level A 未达最低门槛，必须进入 Level B**。
- **Phase 2（固定 65/48 core/block）完成**：`UwbRationalResamplerCcf65_48`
  正确性 100% 达标——7/7 golden 逐样本匹配（max_abs ≤ 4.8e-7）、chunk 不变、
  tag/rx_time/overflow 全过、11/11 CTest、scalar==VOLK。**性能未达**：block 模式
  quality 56.8 / realtime 67.2 input MS/s（0.077/0.091× RT），仅比内置块快
  1.37×。单核 AVX2 理论上限 ≈36 GMAC/s → quality(83 MAC)需 2.3 核、realtime
  (41 MAC)需 1.14 核，**单核无法达实时**。
- **Phase 3（性能优化）完成**：FIR 占 87–97%；宏块+VOLK/AVX2 单核 block realtime
  **88 in MS/s / 0.12× RT**；有序 multi-worker（持久线程池）最优 **workers=20
  realtime → 217 in / 294 out MS/s / 0.29× RT**（~3.1× Phase-2）。**未达最低门槛**
  （需 737 in）。命中停止条件 §12.1（两种独立优化后仍低于实时 15%+）。推荐转向
  **原生率 scheduled 截窗 + PDU 级 65/48** 或 RFNoC。详见报告 §5。CTest 11/11。
- **Phase 3.5（最小阶数滤波器复测）完成**：修正阻带口径为**相对通带 −70 dB**
  （原按绝对 −70 dB 过设计 ~60 dB）。quality → 2707 taps/42 arm、realtime →
  1319 taps/21 arm（~50% 降幅）。golden 7/7 匹配、UWB FCS 回归双档通过。
  实测 realtime_minorder：单核 136.6、8w 250.7、20w 260.7 in MS/s（0.354× RT）——
  **taps 减半单核 +55% 但多核 8→20w 几乎不缩放**，证明多核瓶颈是串行 schedule
  走查/缓存/worker 同步而非 FIR 算力。**连续主机实时路径正式关闭**。
- **主架构转向（PDU 级 65/48）完成**：按建议 §3.2 实现 **`UwbPduRationalResamplerCcf65_48`**
  （Grok Build，run-mso9t4r1-xiy6ay）：737.28 原生率 `UwbScheduledExtractor` 截窗 →
  PDU 65/48 重采样（复用 polyphase core、预分配 scratch、群时延感知坐标映射
  `map(p)=round((p·65+(T−1)/2)/48)`）→ 998.4 PDU → 解调。正确性 12/12 CTest、
  golden max_abs 1.19e-7、e2e **FCS pass**、吞吐 575 pdus/s（200 slot/s 时
  **2.9× 余量**）。Claude 独立 e2e 复验 PASS（detected_start 与 mapped predicted
  相差 28 样本）。连续流与 PDU 两条路径均已闭环；PDU 为主架构，连续流已判死并
  保留正确基线。报告 §7 完整记录。

## 4. 已完成的 Phase-1 基础

- 通用 UWB 检测、固定长度 PDU 截包和异步 PacketWriter。
- `UwbScheduledExtractor`：已知 `t0/T` 时按周期截取 QM35825 slot，默认
  `every_slot`，通信干扰不会放大输出窗口数。
- `UwbAutoScheduledExtractorSc16`：盲 t0、已知 5 ms 时从 SC16@737.28 捕获首个
  FCS-pass QM35 再切 scheduled；mixed 0.5 s 离线已锁定，硬件 soak 未做。
- X410/RFNoC 入口方案：737.28 MS/s 经 65/48 上采样到 998.4 MS/s。
- SC16 整数能量门、Q15/ROI detector、原始 SC16 PDU 与写盘路径。
- QM35825 固定 profile 的 `UwbRealtimeDemodulator`。

专项文档统一归档到 [`docs/phase1/`](docs/README.md#phase-1-与硬件入口)。

## 5. 当前优先级

### 原生率长窗落盘与采集后离线 65/48（本分支已实现）

X410 实时解调、不落盘已通过。旧 `--write-sc16` 曾把同一条 590 µs PDU 送进
65/48，单帧 handler ~7.4 ms，引起 overflow。

[`docs/phase1/开发需求_原生率长窗落盘与离线65_48去单音.md`](docs/phase1/开发需求_原生率长窗落盘与离线65_48去单音.md)
（分支 `feature/native-long-dump-offline-resample`）已落地：

1. `UwbPduWindowCrop`：SC16 域、升采样前按 `predicted_start` 切 10/190/4.1 µs。
   acquisition 直通。`CaptureOnly` 不是本路径。
2. `offline_qm35_auto_lock.py --write-sc16`：extractor 写 590 µs 原生窗；
   FIR 只吃 crop 后的短窗。写出 `demod_results.jsonl`。
3. `uwb_offline_postprocess_dump`：按窗去单音 + 复用 `RationalResampler65_48Core`
   升到 998.4 **SC16**（`capture_998p4.iq`，按窗 `iq_scale`）。默认不覆盖
   `capture.iq`。写盘 0 drop 后自动调用；`--skip-postprocess` 可关；
   工具失败脚本退出码 4。`--out-format cf32` 仍可对照。
   离线按窗线程池（`--workers`，auto 封顶 8）：每线程一份 FIR 核，有序写出。
   去单音用相位递推，不再逐点 `sincos`。不改实时 5 ms 热路径，也不把 FIR
   `nworkers` 嵌进窗级池。

本机 0.5 s 离线回归（2026-08-19）：

| 文件 | 锁 | dump | crop scheduled | FIR mean | writer | live FCS | 后处理 |
|---|---|---|---:|---:|---|---:|---|
| mixed 737.28 | locked，无 t0 | 590 µs SC16 | 99×150479 | 1.73 ms | 100/100 bit-exact，0 drop | 99/100 | 100 窗 → `capture_998p4.iq`（SC16），raw 未改 |
| clean 737.28 | locked，无 t0 | 590 µs SC16 | 98×150479 | 1.84 ms | 99/99 bit-exact，0 drop | 99/99 | 99 窗 → `capture_998p4.iq`（SC16），raw 未改 |

单帧 FIR 从旧长窗 ~4.8 ms 回到短窗 ~1.7 ms，落在 5 ms 槽内。X410 live dump / soak 仍未做。不要写成硬件盲捕获验收通过。

本分支基于本机 `6c137b9`。X410 电脑上的新 `main`（实时解调、不落盘已通过）更新，应在**那台机器**上把
`feature/native-long-dump-offline-resample` 并进去，步骤与冲突判据见
[`docs/phase1/合并说明_原生率长窗落盘与X410实时解调main.md`](docs/phase1/合并说明_原生率长窗落盘与X410实时解调main.md)。
不要用本机旧 main 覆盖那边的实时解调。

离线后处理本机计时（i7-12700，mixed 100 窗 / 173 MB 原生 SC16 → 998.4 SC16，
`quality_minorder`，已知 CW）：

| 配置 | compute | wall | 相对旧串行 ~3.0 s |
|---|---:|---:|---|
| 旧串行（逐点 sincos） | — | ~3.0 s | 1× |
| `--workers 1`（递推去单音） | 1.19 s | 1.78 s | 1.7× |
| `--workers 4` | 0.27 s | 0.71–0.78 s | ~4× |
| `--workers 8` / auto | 0.21 s | 0.82 s | ~3.6× |
| `--workers 20`（不要当默认） | 0.14 s | 3.5 s | 退化 |

`--workers 4` 与 `--workers 1` 的 `capture_998p4.iq` / jsonl **bit-exact**。
auto 封顶 8：每 worker 一份 ~13 MB FIR scratch，20 线程分配/析构把墙钟打回去。

### 锁定后 SC16 截取与 DW1000 头尾预留（离线已实现）

`UwbAutoScheduledExtractorSc16` / `UwbScheduledExtractorSc16` 在锁定后按
EverySlot 截原生 SC16 窗，直写 `UwbPacketWriter`。dump 默认头 300 µs、
尾 100 µs（221184 / 73728 @737.28），QM35 体仍 190 µs。JSONL 含
`window_start` / `predicted_start` / pre/body/post，可切 head / QM35 / tail。

mixed 0.5 s：100 包、**100/100 bit-exact**、writer 0 drop。X410 live dump
未做。不要把 65/48 放进写盘路径。

### 盲捕获自动锁定（硬件验收，未完成）

1. 修正 `qa_uwb_auto_scheduled_extractor_sc16` 的本机模板路径与失败用例。
2. 把 `x410_auto_scheduled_capture.py --identity demod` 接到真实 65/48 + demod。
3. mixed 文件 MATLAB `detected_start` / `t0` 对照。
4. X410：随机启动 20 次、60 s、10 min soak、overflow 后重捕获。

不得因 SC16 写盘完成而把方案 §18 写成硬件盲捕获验收通过。

### 性能线

1. 为 PMT metadata、统计、queue、publish 和 scheduler 建立独立计时。
2. 缓存稳定 PMT key/symbol，减少逐包字典构造与临时对象。
3. 分别验证 result sink 为 drop/debug 时的吞吐，避免把 sink 开销算入 core。
4. 每项优化执行多轮中位数、30 秒 300 pkt/s soak、8/8 CTest 和鲁棒性矩阵。

### SIC 线

1. 先实现无 DW 快路和单 DW direct-first slot orchestration。
2. 再扩展到最多 4 个 DW，并保证每次相减都经过 FCS、相关度和抑制度安全门。
3. 仅在强候选直接解调失败时进入 QM35 fallback。
4. slot core 与真实 mixed golden 闭环后，再实现异步 GNU Radio block。

## 6. 文档入口

- 需求与验收：[`开发需求参考.md`](开发需求参考.md)
- 实时解调架构：[`开发方案_UWB实时解调.md`](开发方案_UWB实时解调.md)
- QM35 盲捕获方案：[`docs/phase1/GROK_X410_QM35盲捕获与自动周期锁定开发方案.md`](docs/phase1/GROK_X410_QM35盲捕获与自动周期锁定开发方案.md)
- 盲捕获离线实测：[`docs/phase1/测试报告_QM35盲捕获与自动周期锁定_mixed737p28.md`](docs/phase1/测试报告_QM35盲捕获与自动周期锁定_mixed737p28.md)
- 锁定后 SC16 落盘与头尾预留：[`docs/phase1/下一步_锁定后SC16截取与DW1000头尾预留.md`](docs/phase1/下一步_锁定后SC16截取与DW1000头尾预留.md)
- 长窗落盘 + 离线 65/48 去单音需求：[`docs/phase1/开发需求_原生率长窗落盘与离线65_48去单音.md`](docs/phase1/开发需求_原生率长窗落盘与离线65_48去单音.md)
- 与 X410 实时解调 main 合并：[`docs/phase1/合并说明_原生率长窗落盘与X410实时解调main.md`](docs/phase1/合并说明_原生率长窗落盘与X410实时解调main.md)
- 性能优化主线：[`Codex_UWB解调器性能优化记录.md`](Codex_UWB解调器性能优化记录.md)
- 历史与专项索引：[`docs/README.md`](docs/README.md)
