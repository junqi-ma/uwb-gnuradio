# UWB GNU Radio 模块——开发状态

> 更新日期：2026-08-10
> 本文档记录 gr-uwb 模块的当前开发状态，作为 `AGENTS.md` / `开发需求参考.md` 的进展对照。

## Phase-2 实时 SIC 状态（2026-08-10，Agent 优先阅读）

**SIC 尚未实现。** 当前已完成的检测、`UwbScheduledExtractor` 周期截取和
`UwbRealtimeDemodulator` QM35825 固定 profile 实时解调是 Phase-1 基础；仓库
目前没有可宣称去除 DW1000 并输出 post-SIC 复数 CIR 的生产 C++ block。

Phase-2 冻结方案见
[`开发方案_GNURadio实时SIC.md`](开发方案_GNURadio实时SIC.md)。目标是在已知
`t0/T` 的扩展上下文 PDU 上执行有界延迟 direct-first SIC，独立保留 raw 分支，
输出约 204 us `cleaned_slot`、复数 `post_sic_cir` 和逐 slot 状态。首版目标
100～200 slot/s，容量至少 250 slot/s，200 slot/s p99 <20 ms。

当前前置阻塞项：

1. **输入契约未闭环**：X410 入口只完成 65/48 重采样；仍缺与 MATLAB 一致的
   同步单音去除和 10 MHz 数字频移及其逐样本验证。
2. **SIC golden 尚不完整**：真实 DW1000 profile、TX pulse impulse、单包重构与
   trial residual 已版本化并由 C++ 复验；仍缺 mixed-signal post-SIC CIR golden。

**S0/S1 已完成的基础（2026-08-10）**：

- 新增 MATLAB R2025b 可再生的 `testdata/sic_profile_golden/`：完整 code-11、
  1016-sample sampled code、Decawave SFD 和 profile manifest；C++ QA 逐元素
  对照 `lrwpan.internal.HRPCodes(11)` 导出结果。
- `Dw1000Profile` 已修正为 code 11 / 128 SYNC / Decawave / 118 CIR repetitions；
  旧 code 10 / 64 SYNC / 4z2 已更名为 `SyntheticCode10Profile`。
- `CirResult::cir_complex_values` 保留归一化复数 taps；旧 `cir_values` 继续提供
  实部诊断。恒定 0.61 rad 复相位 QA 验证虚部未丢失且复数 L2 norm=1。
- 新增 `testdata/dw1000_realtime_golden/`：真实预处理 998.4 MHz code-11 窗口、
  MATLAB 复数 CIR、参考 preamble、字段坐标、12-byte PSDU 与 FCS `0x4440`；C++
  CIR 归一化相关度 0.999998，并完成 timing/CFO/payload/FCS 全链路对照。
- 同一 golden 已增加 178112-sample `{-1,0,+1}` TX pulse impulse、complex-CIR
  replica、字段边界及 trial received/model/residual；S1 完成。

**S2 单包 SIC core（完成，2026-08-10）**：

- 新增无 GNU Radio scheduler 依赖的 `uwb_sic_core.h` 单包 trial/commit primitive：
  整数对齐、逐 SYNC CFO 拟合、全局复增益与实际相减区间抑制度计算。
- FCS、相关度 0.70 和抑制度 0.20 dB 任一安全门失败时保持工作 IQ 逐样本不变；
  QA 覆盖成功相减、FCS bypass、低相关度与低抑制度拒绝。
- 真实单包 trial 的 fitted start=10000、相关度 0.980970、CFO=-2667.709 Hz、
  抑制度 16.2803 dB；C++/MATLAB model 相对 L2 误差 `1.55e-7`，residual 最大误差
  0.00418，相减区间外误差为 0。真实窗口在 FCS、相关度或抑制度门拒绝时均保持
  198140-sample IQ 完全不变。
- 新增 `uwb_tx_reconstructor.h`：从解调后的 19-bit PHR、FCS-valid PSDU 和复数
  CIR 生成 production code-11/128/Decawave pulse impulse 与 replica；包含
  RS(63,55)、CL-3、BPRF scrambler/跳时和 CIR 卷积，内部 scratch 可由 worker
  预分配复用。
- C++ TX impulse 与 MATLAB 178112 个样本完全一致；使用同一 MATLAB CIR 时
  replica 最大误差 `1.33e-7`、相对 L2 `5.19e-8`。完整 C++
  decode→CIR→reconstruct→cancel 抑制度 16.2894 dB，residual 相对 MATLAB L2
  误差 1.19%。S2 完成，下一主线进入 S3 自适应 slot core。

在剩余阻塞项完成前，不得以 profile 常量 QA、近实数旧 golden 或离线 MATLAB
SIC 结果宣称 C++ 实时 SIC 已完成。

**周期旁路专项结论（已完成）**：见仓库根目录
[`开发总结_QM35825周期旁路雷达截取.md`](开发总结_QM35825周期旁路雷达截取.md)
（固定周期雷达时隙截取 + 通信干扰下 every_slot；`UwbScheduledExtractor` Phase-1）。

**实时解调性能优化（P0-P3 全部完成，2026-08-09）**：按
[`下一步性能优化指导_CIR_CFO_SFD.md`](下一步性能优化指导_CIR_CFO_SFD.md) 执行。
单 job（golden 窗口 319168 样本）串行耗时 **17.2 → 3.9 ms（4.4×）**，达成 4-5ms 目标。

**R7 鲁棒性与扩展（2026-08-09，commit 516819b，Grok Build 代做）**：
- `demod-robust` 统计套件（AWGN 30→10dB 100%、CFO ±50kHz 100%、multipath 20/20、碰撞全解+graceful）。
- 历史 code-10 profile（原 `Dw1000Profile`）只有 self-consistency；现已明确
  更名为 `SyntheticCode10Profile`，生产 `Dw1000Profile` 见上述 S0/S1 状态。
- SC16 输入（s16vector 交错 I/Q → c32，FCS 0x584b）。
- GPU/FPGA 评估 + 在线 SIC 记为独立项。
- P0（ea77773）：CFO 用 SYNC 匹配滤波峰值复相位 + unwrap + 相对时间拟合；`stage_sfd` 改用 `scratch.derotated`；新增 CFO sweep（0/±1/±5/±10/±25/±50kHz clean+AWGN）与 CIR 细分计时 QA。
- P1（046e7fe）：CFO 去旋转改 **4 条独立 float 旋转链**（step⁴ + 每 1024 绝对相位重锚定），cfo **3534→436 µs**（8×）。
- P2（1d3e488，**Grok Build**）：新增 `uwb_cir_fir_simd.h`，**固定 38-tap AVX2/FMA 内核**（9×4 SIMD + 2 尾部标量，双累加器，便携 fallback）；3 候选实测 AVX2 1.3ms < VOLK 1.6-1.9ms < MultiAcc8 4.9ms；前向 FIR + 预共轭抽头 + 合并后处理。cir **10839→1720 µs**（6.3×）。
- P3（30e6532）：SFD 搜索中心改 **last_sync_start + round(measured_period)**、半宽分档（全跟踪 ±16 samples），sfd **2112→146 µs**（14×）。
- **验收**：QA 24 用例、CTest 7/7、FCS 0x584b 不变、无 fast-math。**200 pkt/s + 2 workers 零丢包**（原需 4，省 2 核）；p95 22.5 → 9.0 ms。
详细数据见 [`性能分析_解调分阶段耗时报告.md`](性能分析_解调分阶段耗时报告.md)。

**通用截包正确性补强（2026-08-09，commit 624721d）**：
- `UwbDetectorStateMachine` 增加 `CAPTURE` 尾部收集阶段；短能量 Region
  也输出固定 `pre_trigger + capture` 长度，新增跨 chunk 短 Region QA。
- MATLAB 测试波形坐标已严格统一为 0-based：首峰 4,993,015 → packet
  start **4,992,000**，不再保留历史 `+1` 偏差；PDU 增加 `threshold`。
- `UwbPacketWriter` 改为 16-slot 有界异步队列，量化/写盘移出消息处理
  线程，并新增 received/dropped/high-watermark 统计和 Python bindings。
- 验证：CTest 7/7、真实单包逐样本、SC16 writer e2e、100 synthetic
  packets 全通过。
- 已知边界：若两个 packet 的请求 capture 窗本身重叠，当前单活动
  `CAPTURE` 不支持重叠窗口 fan-out；需后续多活动 capture/pending 队列。

**X410/RFNoC 入口与写盘基线（2026-08-09）**：
- 新增 `x410_scheduled_capture.py`：X410 Radio 737.28 MS/s → RFNoC custom
  Upsampler 65/48 → RxStreamer CF32 998.4 MS/s → scheduled extractor → writer。
- 主机侧不做连续流重采样；入口校验固定速率契约与 UHD coerced radio rate。
- 新增 `simulate_scheduled_write.py`；30 秒、200 slot/s、203,776 samples/window
  实测写入 6,000 窗、4,890,624,000 bytes SC16、6,000 JSONL 行。
- 详细使用与实测见 `X410_RFNoC入口与写盘测试.md`；硬件 block ID 与端口待
  X410 到位后用 `uhd_usrp_probe` 确认，`rx_time` 失锁重映射仍未完成。

**SC16 通用检测路径（2026-08-09，已完成）**：
- 新增 `UwbDetectorSc16`：全速整数能量门 + SC16 ring/Region；仅候选在后台
  单次转 CF32 复用既有粗/精相关，原始 SC16 直接输出 s16vector。
- PacketWriter 支持 s16vector 逐 bit 直写，无峰值扫描、重缩放或二次量化。
- 新增 QA、Python bindings、GRC block、X410 SC16 detector flowgraph 和 A/B
  benchmark；SC16/CF32 同为 95/95 packets。
- 500M samples × 3：CF32 中位 169.7 MS/s，SC16 中位 252.7 MS/s；逐轮
  speedup 1.436×/1.439×/1.489×，这是默认小 chunk/plumbing 结果。
- 旧1M chunk结果CF32 743 / SC16 537 MS/s已判定为benchmark source假象：
  `vector_source_s(vlen=2)`逐short循环，source→null本身仅约540–554 MS/s。
- SC16能量门已改为纯整数域 `I²+Q²` + `uint64_t` 滑窗，门限只在构造期
  缩放；gate-only（无粗/细相关、无PDU）5G samples、1M chunk、正反顺序×3：
  CF32中位2386.0 MS/s，SC16中位5993.7 MS/s，逐轮speedup中位2.51×；
  两者每轮952 Regions / 251,575,332 Region samples完全一致。
- benchmark改为FC32/SC16同构bulk memcpy复采样源，并报告source-only基线。
  500M、1M chunk、正反顺序×3：source-only中位FC32 1311.0、SC16 2579.1
  MS/s；完整detector中位FC32 913.6、SC16 Q15/ROI 1550.8 MS/s，逐轮比值
  中位1.700×，均95/95。隔离的“SC16门→整Region FC32→FC32 detector”中位
  1530.2 MS/s；两个SC16后端均超过998.4 MS/s。
- detector→RealtimeDemodulator端到端使用可解调golden窗口（约192 pkt/GS）：
  4 workers六轮中位FC32 910.7、SC16 Q15/ROI 1515.7、SC16门后整Region转
  FC32 1441.7 MS/s；每轮96/96 success、0 fail/drop。SC16 Q15/ROI用2 workers
  三轮中位1496.9 MS/s（约287 pkt/s），queue HWM=1。
- 修复detector→demod PDU坐标：增加`predicted_start_sample`/
  `window_start_sample`；Region history改为`pre_trigger + gate_delay`，避免长
  pre-trigger被门响应延迟截短，并用pre=5000逐样本QA覆盖。
- 详见 `SC16检测器开发与性能.md`。

---

## 1. 总体进展

### 当前结论（Agent 优先阅读）

当前版本已打通“检测 → PDU 截包 → SC16 写盘 → MATLAB 读取”的主链路，现有构建与测试均可通过；但严格对照 `开发需求参考.md`，**能量门 `UwbDetector` 第一阶段尚未完全验收，1 GS/s 生产目标尚未达到**。

另：**QM35825 周期旁路路径（Phase-1）已完成**——按 `t0/T` 截取雷达时隙混合 IQ，默认 every_slot，窗口数不受高通信包率膨胀。详见上述开发总结文档。

后续开发优先处理以下问题：

| 优先级 | 重点问题 | 改进方向 |
|---|---|---|
| P0 | **重叠 capture 尚未支持**：单个短 Region 已保证固定长度，但两个请求窗口重叠时单活动 `CAPTURE` 会抑制后一个候选 | 增加多活动 capture/pending 队列，并定义 pool 满时的显式 drop/backpressure 统计 |
| P0 | **MATLAB 自动调用尚未闭环**：已用 golden 波形精确确认 0-based start=4,992,000 且 IQ 逐样本一致，但 Linux 构建尚不能自动启动 Windows MATLAB | 将 golden 导出结果纳入版本化 manifest，由 Python/CTest 无 MATLAB 环境也可复验 |
| P1 | **需求测试矩阵不完整**：缺 AWGN、SNR sweep、长噪声误报率、CFO sweep、幅度变化及显式 detector chunk-boundary 测试 | 建立统一仿真器和参数化测试，输出 ground truth 与检测统计 |
| P1 | **完整生产吞吐约 135.9 MS/s**，局部 core >1 GS/s 不代表 GNU Radio/PDU 链路达标 | 优先 profile 状态机逐样本循环、region/PDU 复制和热路径分配，再考虑并行/GPU |
| P1 | **接口/配置仍有偏差**：PDU 缺 `threshold`；采样率及 gate/holdoff 参数部分硬编码；动态增大 `coarse_stride` 可能超过 scratch 容量 | 引入集中配置并补全 metadata；setter 同步重建相关缓冲或改为启动前只读配置 |
| P2 | Writer 已异步化但尚未做 200 slot/s、30–60 分钟实盘压力测试 | X410 到位后验证磁盘持续带宽、队列水位、写入延迟和 0 drop |

### ✅ 已完成（主链路）

| 任务 | 状态 | 说明 |
|---|---|---|
| UWB 能量检测块 | ✅ | `uwb.energy_detector`：滑动窗 `\|x\|²` 均值 + 门限标志 |
| UWB 前导码检测块 | ✅ | `uwb.preamble_detector`：归一化匹配滤波；支持 Reference / 能量门 fast / **coarse-to-fine** |
| Fast Detector（能量门） | ✅ | 候选区全速相关，其余置 0；~7.3 → ~61 MS/s（8.4×），与参考逐样本一致 |
| Fast Detector v2（coarse-to-fine） | ✅ | 能量门 D=100 + 粗相关 CD=4–8 + ROI 精相关；**core >1 GS/s（~1037 MS/s）**；dual-output flowgraph ~80–150 MS/s |
| detector_core + 候选区状态机 | ✅ | `uwb_detector_core.h`：纯算法；区域缓冲 `SEARCH → IN_REGION` + 预触发 ring；固定长度 `CAPTURE/HOLD_OFF` 尚待补充 |
| **UwbDetector（生产截包路径）** | ✅ 主链路 | 0 流输出 + `packet` PDU；合并 coarse-to-fine + 跨 chunk 区域缓冲；真实信号 start=4,992,001，IQ 复制 diff=0；逐样本真值对照和短 region 完整截取尚待完成 |
| **Packet Writer** | ✅ | `uwb.packet_writer`：`capture.iq` 为 **SC16**（交错 int16 I/Q）+ `capture.jsonl`（含 `sample_format`/`iq_scale`）；可选 `one_file_per_packet`；PDU 仍为 CF32，落盘时量化 |
| **MATLAB reader** | ✅ | `read_uwb_packet.m`（SC16 + 旧 CF32 兼容）；numpy e2e 量化误差 < 1 LSB（`test_packet_writer.py`） |
| **多包 / 相邻包检测** | ✅ | `test_multi_packet.py`：100 随机短包、5 个真实波形包及 gap=6k/50k 相邻包均全检出；PDU IQ 与其报告区间一致。短 region 的固定 capture 长度不在该测试验收范围内 |
| C++ Boost QA | ✅ **5/5** | energy / preamble / detector / packet_writer / **scheduled_extractor** |
| 真实信号 e2e | ✅ | `test_uwb_detector.py` + `test_packet_writer.py` |
| 构建 / 绑定 / GRC | ✅ | cmake + make + ctest；pybind11；`.block.yml`（含 scheduled_extractor） |
| **UwbScheduledExtractor（周期旁路）** | ✅ Phase-1 | 固定间隔雷达时隙截取；every_slot；混合通信干扰 QA；见开发总结文档 |

### ⏳ 下一阶段

Phase-1 的历史待办继续保留在各专项文档中；当前主线按实时 SIC S0～S5 推进：

1. **S0**：冻结预处理契约、绝对坐标、生产 code-11 profile、复数 CIR schema，
   并继续闭环硬件前端逐样本契约。
2. **S1（完成）**：兼容复数 CIR 接口、生产 DW1000 profile、TX impulse 与逐 tap
   golden 均已闭环。
3. **S2（完成）**：C++ PSDU→TX impulse→complex-CIR replica、trial/commit 和
   真实 residual 对照均已闭环；首版未加入 fractional delay、PLL transient、
   CIR-slow、CFO2、SFO 或字段独立增益。
4. **S3**：实现无 DW 快路、direct-first、多 DW 上限 4、QM35 fallback 和
   post-SIC CIR-only。
5. **S4**：实现 `UwbRealtimeSic` 异步 message/PDU block、有界 worker pool、
   raw 旁路、queue-full/异常恢复及 stop/drain QA。
6. **S5**：闭环 X410 前端预处理并完成 100/200 slot/s 10 分钟 soak、250
   slot/s 容量和 200 slot/s p99 <20 ms 验收。

### 生产推荐路径

**通信包能量检测 / 通用截包：**

```text
IQ 流 → uwb.detector → packet PDU → uwb.packet_writer
                              → capture.iq + capture.jsonl
                              → read_uwb_packet.m
```

**已知雷达周期 t0/T、需在密集通信下保留每个雷达时隙混合 IQ（旁路研究）：**

```text
IQ 流 → uwb.scheduled_extractor → packet PDU（every_slot）
                              → 离线 MATLAB / SIC
```

**Phase-2 目标路径（尚未实现）：**

```text
预处理 IQ → scheduled_extractor 扩展上下文 PDU → realtime_sic
          ├──────────────────────── raw PDU → writer
          └→ cleaned_slot + post_sic_cir + result/status
```

`energy_detector` / `preamble_detector` 保留作调试与对照；**1 GS/s 截包部署不要走 dual-output 全速率 metric 路径**。周期旁路路径使用大 `min_output_buffer` / `max_noutput_items`（约 512k–1M）以发挥 schedule core 吞吐。

---

## 2. 环境

- **平台**：Windows 11 + WSL2 Ubuntu 22.04；构建在 WSL Linux 文件系统（`/home/junqima/workspace/uwb-gnuradio/`）
- **GNU Radio**：3.10.1.1（Ubuntu 包，prefix `/usr`）
- **编译器**：g++ 11.4.0；**CMake** 3.22.1
- **Python**：3.10.12；numpy 1.21.5；**Boost** 1.74
- **MATLAB**：Windows 侧 `H:\MATLAB22`（WSL 内不可直接调用；读包脚本在仓库根目录）

---

## 3. 架构与文件清单

```
uwb-gnuradio/
├── AGENTS.md / 开发需求参考.md / 开发状态.md
├── test_uwb_detector.py           检测 e2e（真实 cfile）
├── test_packet_writer.py          detector → writer → 读回 e2e
├── benchmark_detection_rate.py    preamble 多模式吞吐
├── read_uwb_packet.m              MATLAB 读包
├── gr-uwb/
│   ├── include/gnuradio/uwb/
│   │   ├── uwb_detector_core.h    ★ 纯算法 core
│   │   ├── uwb_energy_detector.h
│   │   ├── uwb_preamble_detector.h
│   │   ├── uwb_detector.h         ★ 生产截包 + PDU
│   │   ├── uwb_packet_writer.h    ★ 写盘
│   │   └── api.h
│   ├── lib/
│   │   ├── uwb_*.cc / qa_uwb_*.cc
│   │   └── CMakeLists.txt
│   ├── apps/benchmark_detector.cc
│   ├── python/uwb/bindings/
│   └── grc/*.block.yml            四个块
└── testdata/
    ├── reference_preamble.bin
    ├── make_reference_template.py
    └── uwb_code9_preamble64_payload128_standard_sfd.cfile
```

---

## 4. 检测算法与关键参数

### 4.1 PHY（MATLAB + cfile 自相关确认）

| 参数 | 值 |
|---|---|
| 采样率 `fs` | 998.4 MHz |
| 平均 PRF | 62.4 MHz（≈每 16 样本一脉冲） |
| SYNC 符号周期 | **1016** samples |
| Preamble | 64 symbols = 65,024 samples |
| SFD | 8 symbols = 8,128 samples |
| 模板 | 首个 SYNC 符号，L2 归一化 |

### 4.2 能量检测

```
energy[j] = (1/W) · Σ |x[j+m]|²     （滑动窗，O(1)/样本）
flag[j]   = energy[j] ≥ threshold
```

### 4.3 前导相关（Reference）

```
metric[j] = |corr[j]|² / (winpow[j] · E_t + eps)
```

- 峰值在**符号结束**位置（`start + L - 1`）。
- GR FIR 为卷积语义：taps 须为 `conj(template)` **逆序**。

### 4.4 UwbDetector 生产管线（合并后）

```
D=100 降采样能量门（跨 chunk）
  → 整段候选区缓冲（含门延迟 pre-buffer）
  → 粗相关（CD=4 推荐）确认前导 + 符号峰
  → 粗峰 ROI 全速精相关
  → packet start = 首符号峰 − (L−1)
  → PDU：[start − pre_trigger, start + capture)
```

默认 `pre_trigger≈2032`、`capture=200000` → 约 **202032 样本**（≈200 μs @ 1 GS/s + 预触发）。

---

## 5. 测试与验证

### 5.1 C++ Boost（`ctest`）—— **4/4 通过**

| 用例 | 覆盖 |
|---|---|
| `qa_uwb_energy_detector` | 窗能量 + flowgraph |
| `qa_uwb_preamble_detector` | 归一化相关 + fast≡reference |
| `qa_uwb_detector` | PDU / 状态机 |
| `qa_uwb_packet_writer` | 写盘 + 元数据 |

### 5.2 真实信号 e2e

输入：`testdata/uwb_code9_preamble64_payload128_standard_sfd.cfile`（5,248,448 samples，包起点 **4,992,000**）。

| 检查项 | 结果 |
|---|---|
| 静默区能量 / 相关 | ≈0 ✅ |
| 包区能量 / 能量门 | 通过 ✅ |
| SYNC 相关峰 | **63** 个，间距 **1016**，峰值 **1.0** ✅ |
| 首峰位置 | sample **4,993,015**（首符号末）✅ |
| UwbDetector PDU | 1 包；start **4,992,001**；metric **1.0**；len **202032** ✅ |
| 截取 IQ vs 真值 | **diff=0** ✅ |
| Writer 读回 SC16（shared + per-packet） | 量化误差 < 1 LSB ✅（`test_packet_writer.py`） |

### 5.3 性能

| 模式 | 吞吐 | 说明 |
|---|---|---|
| 全速率参考相关 | ~7.8 MS/s | 每样本 1016 抽头 FIR |
| 能量门 fast | ~66 MS/s | 8.4× |
| coarse-to-fine（CD=4 / 8） | ~80 / ~118 MS/s | dual-output null sink，plumbing 限 |
| **core 管线** | **>1 GS/s（~1037）** | 算法非瓶颈 |
| **UwbDetector PDU（稀疏，P0/P1 后）** | **~222.6 MS/s（1/s）/ 204.4 MS/s（200/s）** | horizon+first-peak；0 drop；输出流 0.09 / 8.34 MS/s（2026-08-08 §8.5） |
| **Region worker（horizon+first-peak）** | **~201 MS/s Region IQ（1.31 ms/区）** | 相对全 Region 粗扫 **4.14×** |
| **UwbDetector PDU（密集）** | **~35 MS/s**（优化前） | 待用新 worker 复测 dense |
| **能量门检测（状态机）** | **~1.7–2.2 GS/s** | D=100 block loop + bulk ring；**不是瓶颈** |
| **源生成（flowgraph plumbing）** | **~264 MS/s** | PatternSource→null_sink 上限 |

**瓶颈分解（异步流水线后）**：仅能量门 ~2.24 GS/s；完整 detector 稀包 ~160–210 MS/s（随包率变）。能量门非瓶颈。**瓶颈在粗/精相关 + GR 调度/缓冲（source buffer 常被钳到 8192）**。能量门全量 VOLK 实测负结果（16% 稀疏 norm 已是优解）。

**发包率端到端（P0/P1 后，2026-08-08，见报告 §8.5）**：

| 发包率 | gap | 占空比 | 仅能量门吞吐 | **全流水线输入吞吐** | **输出 PDU IQ 流** | det / drop |
|---|---|---|---:|---:|---:|---|
| 200/s | 4,735,552 | 5.14% | 2095 MS/s | **204.4 MS/s** | **8.34 MS/s** | 101 / 0 |
| 100/s | 9,727,552 | 2.57% | 2137 MS/s | **212.7 MS/s** | **4.38 MS/s** | 51 / 0 |
| 10/s | 99,583,552 | 0.257% | 2247 MS/s | **203.7 MS/s** | **0.49 MS/s** | 6 / 0 |
| 1/s | 998,143,552 | 0.0257% | 1749 MS/s（1G） | **222.6 MS/s** | **0.090 MS/s** | 2 / 0 |

相对 §4.2 基线：200/s 输入 **151.6→204.4**（+35%），输出 **6.19→8.34** MS/s；1/s 输入 **189→223**。输出流与包率近似线性（每包 202,032 samples）。仍未达 1 GS/s（~20–22% of real-time）；稀包逼近 PatternSource plumbing ~264 MS/s。

**Region IQ 容量分层（2026-08-08，见报告 §8.1–8.2）**：

| 层级 | Region IQ 速率 | 说明 |
|---|---:|---|
| 能量门物化（密集） | ~880 MS/s | 仅缓冲，无粗/精 |
| **粗/精 core（持续 Region 块）** | **~45–48 MS/s** | `detector-region`；~5.5 ms/区，~180 Region/s |
| 完整 detector 密集流 | ~35 MS/s | `detector-region-stream`；0 drop |
| 门输出 @1 包/s（壁钟） | ~0.7–1.2 MS/s | 远低于 worker 容量（~40× 余量） |

1 包/s 时 Region IQ **不是**瓶颈；worker 上限约 48 MS/s ≈ 180 包/s @ 264k Region。

**部件耗时（优化前 `detector-profile`）**：粗相关 **87%** → 精相关 8% → PDU 5%。详见报告 §8.3。

**P0/P1 已落地（2026-08-08）**：preamble-horizon 粗扫 + first-peak fine。`detector-region` **5.442 → 1.315 ms/region（4.14×）**，Region IQ **48.5 → 200.8 MS/s**。D=8 冒烟通过但**默认仍 D=4**。详见 `测试报告_发包率扫描.md` §8.4。

**300 pkt/s端到端验证（2026-08-10）**：按998.4 MS/s设置3,328,000
samples/packet，连续900包。SC16 detector→FC32 demod在2 workers下达到
1295.6 MS/s / 389.3 pkt/s，900/900成功、0 drop、queue HWM=4；30秒定速
demod soak为9000/9000成功、0 drop、queue HWM=2。FC32完整入口仅800.5
MS/s / 240.5 pkt/s，不能满足该实时目标。

**已尝试并实测的优化结论**：
- **粗相关鲁棒 stride：不可行**。降采样匹配滤波峰极窄（对齐处 1.0、±1 dec 0.28、±2 dec 0.05、±4 dec ~0），步进扫描会整体漏峰。已保留 stride=1（全扫）并加注释。
- **单独调大源/块缓冲无提升**：PatternSource 请求 4M 输出 buffer 仍被 GNU Radio 钳到 8192。
- **有效优化**：粗相关 VOLK、状态机 block loop + bulk ring（gate ~2.24 GS/s）、预分配 region 池、后台相关/PDU、`-march=native`。

### 5.4 Benchmark 工具

```bash
# preamble 多模式
PYTHONPATH=gr-uwb/build/test_modules LD_LIBRARY_PATH=gr-uwb/build/lib \
  python3 benchmark_detection_rate.py

# C++ 独立：reference|fast|coarse | detector-gate|detector-sparse|detector-dense
#          | detector-region | detector-region-stream
gr-uwb/build/apps/benchmark_detector <cfile> <mode> [--target N] [--gap G] [--regions N]
# 例：UwbDetector 稀疏 1e9 样本
gr-uwb/build/apps/benchmark_detector <cfile> detector-sparse --target 1000000000
# 最大 Region IQ（持续 Region 块 → 粗/精）
gr-uwb/build/apps/benchmark_detector <cfile> detector-region --regions 1000
# 完整块持续 Region 压力
gr-uwb/build/apps/benchmark_detector <cfile> detector-region-stream --target 50000000 --gap 6000
# 部件耗时剖析
gr-uwb/build/apps/benchmark_detector <cfile> detector-profile --regions 200
```
`detector` 模式：流式 PatternSource 生成 [真实包+间隔]（不存全流，可测 1e9）+ PDU counter；报告 MS/s / GB/s / CPU% / detections / 是否达到 1 GS/s。

---

## 6. 构建与运行

```bash
cd ~/workspace/uwb-gnuradio/gr-uwb/build
cmake ..
make -j$(nproc)
ctest

cd ~/workspace/uwb-gnuradio
PYTHONPATH=gr-uwb/build/test_modules LD_LIBRARY_PATH=gr-uwb/build/lib \
  python3 test_uwb_detector.py
PYTHONPATH=gr-uwb/build/test_modules LD_LIBRARY_PATH=gr-uwb/build/lib \
  python3 test_packet_writer.py

# 模板
cd testdata && python3 make_reference_template.py

# staging 安装
cd gr-uwb/build && make install DESTDIR=$HOME/gr-uwb-stage
```

---

## 7. 已知问题 / 注意事项

1. **`sndfile::sndfile` 桩**：Ubuntu gnuradio 包缺 target 时顶层 CMake 用 INTERFACE IMPORTED；装 `libsndfile1-dev` 后可去掉。
2. **`pmt::init_c32vector`**：禁止标量 `0` 字面量（绑到指针重载）；须传 `vector`（见 `uwb_detector.cc`）。
3. **metric+flag 双输出块**（energy / preamble）：两路都必须连接。
4. **GR 3.10 Python connect**：用 `(block, port)` 元组。
5. **能量门阈值**依赖电平（测试包能量 ~0.024，阈值 1e-3 可用）；换功率需标定。
6. **`preamble_detector` 流式 coarse-to-fine** 在 chunk 边界仍可能偶发丢 1 峰；**截包用 `uwb.detector`**。
7. **`UwbDetector` 的 `stop()` 发 PDU 不保证送达**：无尾部静默时末包可能丢；测试/采集宜留尾静默。
8. **粗相关 CD**：CD=4 最稳，CD=8 可用，**CD=16 不可靠**；流式 `coarse_repetitions` 默认 1，整段捕获可 ≥8。
9. **多包 / 相邻包**：已验收（需求 #2/#5）。能量门为 **sum16 邻域和**（每 D 块内前 16 样本 `|x|²` 之和，相位无关、0% 跌穿），区域用**队列**承接多个完成区。**最小包间隔 ≈ 门窗 3200 + holdoff 800 = ~4000 样本（≈4 µs @ 998.4 MHz）**；更小的间隔会合并为同一 region/PDU。
10. **高包率写盘**：当前 Writer 在消息线程同步写；~200 包/s × ~1.6 MB 时可能需异步队列（未实现）。

---

## 8. 下一步（按执行优先级）

| 阶段 | 项 | 完成标准 |
|---|---|---|
| S0 | 输入/profile/schema/golden 冻结 | 998.4 MHz 预处理契约和 0-based 坐标明确；code-11/128/Decawave、复数 CIR 与 golden manifest 可复验 |
| S1 | 复数 CIR + production DW profile | 保持旧诊断兼容；复数 taps、TX impulse 和 profile 逐项通过 MATLAB golden |
| S2 | 单包 SIC core | FCS、相关度 ≥0.70、抑制度 ≥0.20 dB 三重门；重构、区间和 residual 逐样本对照 |
| S3 | 自适应 slot core | 无 DW 快路、direct-first、QM35 fallback、最多 4 DW、post-SIC CIR 矩阵 QA 通过 |
| S4 | 异步 `UwbRealtimeSic` block | handler 仅校验/有界入队；scratch 复用；queue-full/stop/drain/异常均不阻塞且原样旁路 |
| S5 | 性能与硬件闭环 | 100/200 slot/s 各 10 分钟 0 job/raw drop；容量 ≥250 slot/s；200 slot/s p99 <20 ms |

> 第 1～7 节中仍出现的“payload 解调不在第一阶段”是检测/截取 Phase-1 的
> 历史范围说明。固定 profile 实时解调已成为 Phase-1 基础；当前主目标是上述
> Phase-2 SIC，完整补偿、连续净化 stream 和 GPU/FPGA 仍不在首版范围。
