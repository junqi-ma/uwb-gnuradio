# GNU Radio 高速 UWB Packet 检测与截取模块——开发需求

## 1. 目标

开发一个 GNU Radio OOT C++ 模块，处理连续高速复数 IQ 流：

```text
IQ Stream → 检测 UWB Packet → 截取 Packet 附近 IQ → 写入磁盘
```

核心原则：

- 未命中的原始数据立即丢弃。
- 只保存检测到的 packet，不保存连续 1 GS/s 原始流。
- C++ 只负责检测、定位、截取和存储。
- 解调、精同步、CFO 补偿、CSI、ToA、PDOA 等留给 MATLAB。
- 第一阶段优先保证正确性和架构，1 GS/s 实时处理是后续优化目标。

## 2. 开发环境

- Windows + WSL2 Ubuntu
- GNU Radio、GCC/G++、CMake、Ninja、Python
- 暂不连接真实 USRP
- 项目、构建目录和 benchmark 数据放在 WSL Linux 文件系统，不放在 `/mnt/c`
- 不绑定特定 GNU Radio 小版本

程序启动和 benchmark 时输出：

```text
GNU Radio version
compiler version
CPU
sample format
sample rate
```

## 3. 输入与仿真

第一阶段仅支持：

```cpp
std::complex<float>   // GNU Radio gr_complex / CF32
```

目标采样率约 1 GS/s；后续再考虑 SC16。

测试使用已知波形：

```text
reference_packet.bin
或 reference_preamble.bin
```

仿真器生成 AWGN，并随机插入波形，支持配置：

- packet 位置和数量
- SNR
- 幅度
- CFO（可选）
- timing offset（可选）

同时输出 ground truth：

```text
packet_start_sample
packet_length
snr
cfo
amplitude
```

测试数据使用短流或内存流，不生成数秒长的 1 GS/s 文件。

## 4. 软件架构

至少拆分为：

```text
detector_core    独立检测与截取逻辑
GNU Radio block  调度适配及 PDU 输出
packet_writer    磁盘写入
simulation       仿真数据生成
tests            自动化测试
benchmark        独立性能测试
matlab           数据读取脚本
```

强制要求：

- `detector_core` 不依赖 GNU Radio。
- 检测算法不得全部写入 GNU Radio block。
- detector 的 `work()` 不执行大量文件 I/O。
- detector 状态必须跨多次 `process()`/`work()` 调用保持。
- 所有绝对 sample index 使用 `uint64_t`。

建议核心接口：

```cpp
struct DetectorConfig {
    double sample_rate;
    float detection_threshold;
    size_t coarse_stride;
    size_t fine_search_window;
    size_t pre_trigger_samples;
    size_t capture_samples;
    size_t holdoff_samples;
    bool energy_gate_enable;
    float energy_threshold;
    bool debug_enable;
};

class UwbDetector {
public:
    void process(
        const std::complex<float>* input,
        size_t count,
        uint64_t absolute_sample_index);
};
```

所有参数集中管理，禁止散落 magic number。

## 5. 检测与截取

使用状态机：

```text
SEARCH → CAPTURE → HOLD_OFF → SEARCH
```

- `SEARCH`：寻找已知 UWB preamble。
- `CAPTURE`：取回触发前样本，并收集后续样本。
- `HOLD_OFF`：防止同一 packet 重复触发。

必须维护 ring buffer。若估计 packet 起点为 `N`，输出范围为：

```text
[N - pre_trigger_samples, N + capture_samples)
```

必须正确处理：

- preamble 跨两个或多个输入 chunk
- packet 跨多次 GNU Radio `work()` 调用
- 捕获过程跨 chunk
- 两个间隔较短的合法 packet

不得假设 packet 完整位于单个输入 buffer 中。

## 6. 检测算法

第一阶段实现正确性优先的 Reference Detector，使用 normalized cross-correlation：

```text
metric = |Σ x·conj(s)|² /
         ((Σ|x|²)(Σ|s|²) + epsilon)
```

Reference Detector 用于：

- 验证检测位置和截包结果
- 生成性能/算法对照基准
- 验证后续优化版本结果未发生错误

完成第一阶段后，再实现 Fast Detector：

```text
可关闭的能量门限
→ 粗检测（降采样、stride、短模板等）
→ candidate 附近全速精细相关
→ 精确 packet start
```

未经 benchmark/profile，不提前引入 GPU、FFT 或复杂多线程。

## 7. GNU Radio 接口

第一版采用：

```text
stream input → UWB Detector → packet PDU → Packet Writer
```

PDU payload 为原始 CF32 IQ，metadata 至少包括：

```text
packet_id
start_sample
trigger_sample
sample_rate
sample_count
detection_metric
threshold
pre_trigger_samples
```

可扩展字段：

```text
rx_time
coarse_cfo
```

## 8. 存储格式

默认使用两个连续文件：

```text
capture.iq       连续保存各 packet 的 CF32 IQ
capture.jsonl    每行一条 metadata
```

JSONL 至少记录：

```json
{
  "packet_id": 1,
  "start_sample": 123456789,
  "file_offset_samples": 0,
  "sample_count": 200000,
  "sample_rate": 1000000000,
  "detection_metric": 0.91
}
```

调试模式可以支持：

```text
one_file_per_packet = true
```

提供 MATLAB helper：

```matlab
[x, meta] = read_uwb_packet(
    "capture.iq",
    "capture.jsonl",
    packetId);
```

## 9. 自动化测试

至少覆盖：

1. 纯 AWGN：不得误报。
2. 单 packet：检测起点误差不超过配置的 tolerance。
3. 随机插入 100 个 packet：自动对比 ground truth。
4. preamble 和 packet 跨输入 chunk。
5. 两个相邻 packet：holdoff 不得吞掉第二个合法 packet。
6. SNR sweep：统计检测率。
7. 长时间纯噪声：统计 false triggers/second。
8. CFO sweep：测量可容忍 CFO 范围。
9. 幅度变化：normalized metric 应基本稳定。
10. 截包验证：输出 IQ 必须与 ground truth 对应区间逐样本一致。

## 10. Benchmark

建立独立的 `benchmark_detector`，输入在内存中生成，排除磁盘 I/O 干扰。

输出：

```text
processed samples
elapsed time
throughput MS/s
throughput GB/s
CPU utilization
detections
```

分别测试相当于：

```text
100 / 250 / 500 / 750 / 1000 MS/s
```

只有处理 10 亿样本耗时不超过 1 秒，才视为达到 1 GS/s。

优化顺序：

```text
正确性
→ 降低算法复杂度
→ 减少内存复制
→ -O3 / -march=native
→ SIMD / VOLK
→ 缓存与内存布局
→ 多线程
→ GPU
```

## 11. 第一阶段验收标准

第一阶段完成需同时满足：

- OOT 模块可在 WSL2 编译、安装和运行。
- 可读取仿真或文件 IQ 流。
- Reference Detector 可检测已知 UWB 波形。
- chunk boundary 测试通过。
- ring buffer 可准确恢复触发前数据。
- 截取 IQ 与 ground truth 逐样本一致。
- 只保存 packet，不保存连续原始流。
- `capture.iq` 和 `capture.jsonl` 可由 MATLAB 正确读取。
- 自动化测试全部通过。
- 已建立 standalone benchmark，并记录当前性能。

达到以上标准后，再开始 Fast Detector 和接近 1 GS/s 的优化。

## 12. 明确不在第一阶段实现

```text
payload demodulation
BPM-BPSK / FEC / STS decoding
channel estimation
precise CFO compensation
precise ToA ranging
PDOA
packet content parsing
复杂 GUI
真实 USRP 接入
```

## 13. 开发顺序

严格按以下顺序推进：

```text
1. 创建 OOT 工程
2. 实现 standalone detector_core
3. 实现 Reference Detector
4. 实现 ring buffer 和状态机
5. 完成仿真及核心单元测试
6. 完成 chunk boundary 测试
7. 封装 GNU Radio block
8. 输出 packet PDU
9. 实现 Packet Writer
10. 生成 capture.iq 和 capture.jsonl
11. 实现 MATLAB reader
12. 建立 benchmark
13. profile
14. 设计 Fast Detector
15. SIMD/VOLK 等优化
16. 测试能否接近 1 GS/s
```

每个步骤必须先完成：

```text
build → test → commit
```

如果测试失败，先修复当前阶段，不得跳到后续优化。