# 开发总结：QM35825 周期旁路雷达包提取（有通信干扰）

> **Phase-2 交接（2026-08-10）**：本总结冻结的是 scheduled extraction
> Phase-1。实时 DW1000 SIC 将复用 `UwbScheduledExtractor`，但要求输出左右
> 扩展的上下文 PDU；设计见
> [`开发方案_GNURadio实时SIC.md`](开发方案_GNURadio实时SIC.md)。本文的“在线
> 完整 SIC 未做”是 Phase-1 状态，不再表示项目长期非目标。

> 更新日期：2026-08-09
> 依据方案：`GROK_X410_QM35825周期旁路开发方案.md`（Phase-1）
> 下一步：`下一步开发计划_QM35825周期旁路.md`
> 算法对照：`UWB_demodulation/run_decode_uwb_radar.m`、`decode_uwb_all.m::predictFixedIntervalCandidates`

---

## 0. 基线固化（Phase-1 freeze）

按 `下一步开发计划_QM35825周期旁路.md` §P0「固化 Phase-1」：

| 项 | 状态 |
|----|------|
| schedule core + GR block + QA + GRC + bindings | 已入库 |
| `benchmark_detector` scheduled-extractor / scheduled-mixed | 已入库 |
| 方案 / 开发总结 / 状态 / AGENTS 文档 | 已入库 |
| 完整 CTest 5/5 | 固化时通过 |
| `scheduled-mixed` bench（真实 code-9 + code-10 叠加） | 固化时通过 |
| `git diff --check` 无空白错误 | 固化时通过 |
| 提交不含 `.orig/.rej`、不含测试二进制产物 | 是 |
| Git 提交 | `Finalize scheduled extractor Phase-1`（见 `git log --grep`） |

**回退方式**：`git log --oneline --grep 'scheduled extractor Phase-1'` 取 hash 后
`git revert <hash>`，或 `git checkout <hash>^ -- gr-uwb/` 相关路径。
**不破坏**：既有 `UwbDetector` 能量门路径独立保留。

**复现命令**（仓库根目录）：

```bash
cd gr-uwb/build && cmake --build . -j && ctest --output-on-failure

# 混合通信干扰吞吐（需 code-9 雷达 cfile + code-10 通信 cfile）
./apps/benchmark_detector \
  ../../testdata/uwb_code9_preamble64_payload128_standard_sfd.cfile \
  scheduled-mixed \
  --target 50000000 --radar-rate 200 --comm-rate 2000 \
  --buffer-items 1048576 --max-noutput-items 1048576 --repeat 1
```

---

## 1. 目标与结论

| 目标 | 状态 |
|------|------|
| 按雷达周期 `t0 + k·T` 截取固定长度混合 IQ | ✅ 完成 |
| 默认 `every_slot`：验证失败 / 碰撞也不丢窗 | ✅ 完成 |
| 窗口数只跟雷达 schedule，不受 ~2000 通信包/s 膨胀 | ✅ 完成 |
| PDU IQ 与源流逐样本一致 + 元数据 | ✅ 完成 |
| 窗外 bulk skip，不做全速率能量门 / 逐通信相关 | ✅ 完成 |
| 与 MATLAB 固定周期候选坐标规则一致 | ✅ 完成（0-based） |
| 不改坏现有 `UwbDetector` 产线 | ✅ CTest 全绿 |

**一句话结论**：在密集通信干扰下，按 QM35825 雷达周期从连续 CF32 IQ 中稳定抽出每个雷达时隙的**原始混合 IQ**，供离线 MATLAB / SIC；实时计算量与雷达率（100–200/s）成正比，而不是与通信包率（可达 2000/s）成正比。

**Phase-1 已完成。** 自动捕获、`rx_time` 失锁重映射、时钟跟踪等属 Phase-2+，未纳入本交付。

---

## 2. 交付物

| 类型 | 路径 |
|------|------|
| 调度 core（GNU Radio 无关） | `gr-uwb/include/gnuradio/uwb/uwb_scheduled_extractor_core.h` |
| GR block 头文件 | `gr-uwb/include/gnuradio/uwb/uwb_scheduled_extractor.h` |
| GR block 实现 | `gr-uwb/lib/uwb_scheduled_extractor.cc` |
| QA | `gr-uwb/lib/qa_uwb_scheduled_extractor.cc` |
| GRC | `gr-uwb/grc/uwb_scheduled_extractor.block.yml` |
| Python bindings | `gr-uwb/python/uwb/bindings/python_bindings.cc` |
| 吞吐 bench | `gr-uwb/apps/benchmark_detector.cc`（模式 `scheduled-extractor` / `scheduled-mixed`，真实 code-10 叠加） |
| 通信样本生成器 | `testdata/generate_uwb_comm_sample.m`（MATLAB UWB_demodulation 库，`CodeIndex=10`）→ `testdata/uwb_code10_preamble16_payload8.cfile` |
| 方案原文 | `GROK_X410_QM35825周期旁路开发方案.md` |

**未替换**：现有 `UwbDetector` / energy / preamble / packet_writer 生产路径保持不变。

---

## 3. 设计要点

### 3.1 坐标与窗口

```text
predicted_start(k) = round(t0 + k × T)     # 每槽独立计算，禁止累计取整 period
window = [predicted - pre_guard, predicted + capture + post_guard)
```

- 与 MATLAB `predictFixedIntervalCandidates` 一致：
  `candidates = round((first_packet_time_s + k * packet_interval_s) * fs_rx)`
- 坐标统一为 **0-based absolute sample**；与 MATLAB 文件索引（1-based）对照时需 ±1 换算
- 默认窗参数（可配置）：pre ≈ 10 µs，capture ≈ 190 µs，post = 4096 samples（@ 998.4 MS/s）

### 3.2 默认发射策略

- `emit_policy = every_slot`（研究默认）：雷达相关失败、仅有通信能量、双 metric 低、疑似 collision **均输出**固定长度 PDU，并在 metadata 标记原因
- 可选 `verified_only`：仅验证通过才输出（生产筛选，**不是**干扰研究默认）

### 3.3 热路径语义

```text
UNLOCKED →（外部 seed t0/T）→ WAIT_WINDOW → COPY_WINDOW → ENQUEUE → WAIT_WINDOW
```

- `work()`：计算 chunk 与下一预测窗交集；窗外 bulk skip，窗内 bulk `memcpy` 进预分配池
- 完成后只入队 handle；PMT、可选相关验证、`message_port_pub` 在后台 worker
- 禁止在 `work()` 中：vector 扩容、相关、文件 I/O、长时间等 worker

### 3.4 线程安全（实现约束）

- `pool_mutex_` 保护 free list 与 ready 队列（对齐 `UwbDetector` 池模式）
- `checked_out`：worker 独占槽位直至 `release_window`；configure/reset 不擦除持有中 IQ
- block 侧 reconfigure 前：`enqueue_ready` + `wait_for_worker_idle()`
- 计数器使用 `std::atomic`（scheduled / completed / emitted / dropped 等）

---

## 4. Block 接口摘要

```text
gr::uwb::UwbScheduledExtractor   (gr::sync_block)
  流输入：1 × CF32
  流输出：0
  消息输出：packet、status
  消息输入：schedule   # set | reset | pause | resume
```

**主要构造参数**

| 参数 | 说明 | 典型默认 |
|------|------|----------|
| `sample_rate` | 输入采样率 | 998.4e6 |
| `packet_interval_s` | 雷达周期 T | 0.01（100/s）或 0.005（200/s） |
| `first_packet_sample` | 首包前导起点 t0（0-based） | 外部 seed |
| `pre_guard_samples` | 预测起点前保护 | ~9984（10 µs） |
| `capture_samples` | 预测起点后截取主体 | ~189696（190 µs） |
| `post_guard_samples` | 尾保护 | 4096 |
| `pool_size` | 预分配窗槽 | 8 |
| `emit_policy` | every_slot / verified_only | every_slot |
| `verification_enabled` | 后台模板检查 | false（Phase-1 可关） |

**PDU metadata（至少）**：`schedule_index`、`predicted_start_sample`、`window_start_sample`、`window_end_sample`、`sample_count`、`sample_rate`、`packet_interval_s`、`radar_verified` / `radar_metric`、`comm_present` / `comm_metric`、`collision`、`partial`、`schedule_generation` 等。Payload 为 CF32。

---

## 5. 验证与实测

### 5.1 CTest

```text
5/5 PASSED
  uwb_qa_uwb_energy_detector
  uwb_qa_uwb_preamble_detector
  uwb_qa_uwb_detector
  uwb_qa_uwb_packet_writer
  uwb_qa_uwb_scheduled_extractor   # 新增
```

### 5.2 QA 覆盖要点

| 覆盖项 | 说明 |
|--------|------|
| 独立 round vs 累计取整 | 非整数 period 长序列不漂移 |
| 跨 chunk / 单 chunk 多时隙 | 边界正确；IQ 与源逐样本一致 |
| 静默时隙仍调度 | 无能量也 every_slot |
| FG 多槽 + EOS | PDU 序号单调；末完整窗可送达 |
| 验证失败仍 emit | every_slot 不因 miss 丢窗 |
| 混合雷达 + 通信 | 通信 0 / 500 / 2000/s **窗口数相同** |
| 前导碰撞（复数相加） | collision 时隙仍输出 + metadata |
| 并发 pool take/release | completed == released；无 free-list 损坏 |
| MATLAB 坐标公式 | `round((t0+kT)*fs)` 一致 |

### 5.3 吞吐（本机诚实记录）

| 场景 | 结果 |
|------|------|
| CORE（无 GR），200 radar/s + 2000 comm | 约 2.9–3.1 GS/s（真实 code-10 复数叠加，验证不计时） |
| GR e2e + 1M buffer / max_noutput | 约 2.5–3.3 GS/s（真实叠加：2000 comm → 3.06 GS/s、500 → 2.47、0 → 3.32） |
| GR e2e + 默认小 chunk（~4k） | 约 240–250 MS/s（受 GR plumbing 限制，非 schedule 算法） |
| 通信率 0 vs 500 vs 2000 | 窗口数相同（21=21=21）；吞吐不变（CORE ~2.9–3.1 GS/s） |

> `scheduled-mixed` 已从"稀疏脉冲占位"补全为 **code-10 真实复数叠加**：`testdata/uwb_code10_preamble16_payload8.cfile` 由 MATLAB UWB_demodulation 库（`lrwpanWaveformGenerator`, `CodeIndex=10`, 16 SYNC）生成，`MixedPattern` 在雷达时隙与 `fs/comm_rate` 位置上做逐样本 IQ 相加。CORE 路径对每个窗口额外报告 `radar_verified / comm_present / collision`（2000/s 时 10:1 周期比致每窗精确碰撞 → 20/20/20；500/s 时 2.5:1 → 20/10/10），全部仍按 `every_slot` 输出。GR 计时路径关闭块内验证以免 EOS 排空污染吞吐；块级验证/collision 由 QA 与 CORE pass 覆盖。

> 方案验收写的 ≥1 GS/s：在 **大 `min_output_buffer` + `max_noutput_items`** 下 GR 路径可达到；默认小 chunk 时受 scheduler 限制。CORE 本身远超 1 GS/s。

### 5.4 构建与复现

```bash
cd gr-uwb/build
cmake --build . -j
ctest --output-on-failure

# 混合干扰吞吐
./apps/benchmark_detector unused scheduled-mixed \
  --target 100000000 --radar-rate 200 --comm-rate 2000 \
  --buffer-items 1048576 --max-noutput-items 1048576 --repeat 1
```

### 5.5 Python 用法示例

```python
from gnuradio import uwb

ext = uwb.scheduled_extractor(
    998.4e6,              # fs
    0.01,                 # T：100 radar/s（0.005 → 200/s）
    first_packet_sample,  # t0，0-based
    9984,                 # pre_guard
    189696,               # capture
    4096,                 # post_guard
    8,                    # pool
    uwb.scheduled_extractor.EmitPolicy.EverySlot,
    False,                # verification_enabled
)
# stream → ext；msg_connect(ext, "packet", sink, "store")
```

---

## 6. 验收对照（Acceptance）

| # | 要求 | 结论 |
|---|------|------|
| 1 | 独立 `round(t0+kT)` 预测窗 | ✅ core + MATLAB 公式 QA |
| 2 | 默认 every_slot | ✅ miss / collision 仍 PDU |
| 3 | 窗数 = 雷达 schedule only | ✅ mixed 0/500/2000（真实 code-10 叠加） |
| 4 | IQ 保真 + metadata | ✅ FG / mixed 逐样本 |
| 5 | 窗外不跑全速率能量-区域相关管线 | ✅ bulk skip；bench 对 comm 不敏感（CORE ~2.9–3.1 GS/s 不随 comm 率变） |

---

## 7. 明确未做（Phase-2+ / Non-goals）

- 实时完整解调全部 ~2000 通信包/s
- 在线完整 SIC；PDU 改为 SC16
- 把通信包也输出为 PDU
- 靠抬高能量门限区分雷达 / 通信
- 删除严重干扰但未验证成功的时隙
- UHD `rx_time` 不连续重映射
- alpha-beta 时钟跟踪 / 自动捕获与重捕获
- 替换现有稳定 `UwbDetector` 能量门路径

其中“在线完整 SIC”已提升为当前 Phase-2 主目标，但在本文 Phase-1 基线中仍
保持未实现。其输入不是当前约 204 us 输出窗直接原地处理，而是左右包含最大
DW1000 帧和 128-sample 保护的约 660 us 上下文；净化结果再裁回目标 slot。

---

## 8. 建议后续工作

1. 真实 X410 采集 + 已知 `t0/T`，与 `run_decode_uwb_radar.m` 逐 slot 对照
2. Phase-2：`rx_time` tag 映射与 discontinuity → `schedule_lost`
3. 后台 code-9 / code-10 分类默认打开（**仍保持 every_slot**）
4. 可选 alpha-beta / 稳健线性拟合跟踪长采集漂移
5. 按方案建议拆 commit 入库（core → block → mixed bench）

---

## 9. 与其它文档关系

| 文档 | 关系 |
|------|------|
| `GROK_X410_QM35825周期旁路开发方案.md` | 本总结对应其 **Phase-1** 完成状态 |
| `开发状态.md` | 模块全局进度；本文为周期旁路专项结论 |
| `开发方案_GNURadio实时SIC.md` | Phase-2 上游/下游契约、direct-first SIC、安全门与验收 |
| `性能分析测试报告_buffer与分层吞吐.md` | 缓冲 / work chunk 与 1 GS/s plumbing 背景 |
| `UWB_demodulation/run_decode_uwb_radar.m` | 离线固定周期解码对照入口 |

---

**文档状态**：Phase-1 开发完成并经 CTest / 混合干扰 / 并发池 / 吞吐 bench 验证。可接入连续 CF32 流，按雷达 schedule 输出研究用混合 IQ PDU。
