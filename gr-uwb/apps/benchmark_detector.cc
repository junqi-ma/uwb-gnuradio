/* -*- c++ -*- */
/*
 * Copyright 2026
 *
 * SPDX-License-Identifier: GPL-3.0-or-later
 *
 * Standalone detector benchmark (开发需求参考.md §10).
 *
 * Input is generated IN MEMORY (the reference UWB test signal repeated), so
 * the measured time is pure C++ work() throughput — no disk I/O, no Python,
 * no output materialization (null sinks).  Reports processed samples, elapsed,
 * throughput in MS/s and GB/s, and detections.
 *
 * scheduled-extractor / scheduled-mixed superpose a REAL code-10 comm burst
 * (testdata/uwb_code10_preamble16_payload8.cfile, generated by the MATLAB
 * UWB_demodulation library) onto the radar stream via complex IQ addition;
 * window count stays schedule-only and collisions are detected yet still
 * emitted (every_slot).
 *
 * Usage:
 *   benchmark_detector <cfile> <mode> [options]
 *     mode:
 *       reference | fast | coarse
 *       source-null             — PatternSource → null_sink (GR plumbing baseline)
 *       source-search           — PatternSource → UwbDetector (never-trigger thr)
 *       detector-gate | detector-sparse | detector-dense
 *       detector-region         — continuous Region IQ → coarse/fine (core)
 *       detector-region-stream  — min-gap packet stream → full UwbDetector
 *       detector-profile        — per-stage wall-time breakdown + advice
 *     options: --reps N --energy-dec D --coarse-dec D --target N --gap G
 *              --regions N  (detector-region/profile, default 200)
 *              --buffer-items N --max-noutput-items N
 *              --source pattern|vector --threshold VALUE --repeat N
 */

#ifdef HAVE_CONFIG_H
#include "config.h"
#endif

#include <gnuradio/blocks/null_sink.h>
#include <gnuradio/blocks/vector_source.h>
#include <gnuradio/filter/fir_filter.h>
#include <gnuradio/top_block.h>
#include <gnuradio/uwb/uwb_detector.h>
#include <gnuradio/uwb/uwb_detector_core.h>
#include <gnuradio/uwb/uwb_preamble_detector.h>
#include <gnuradio/uwb/uwb_scheduled_extractor.h>
#include <gnuradio/uwb/uwb_scheduled_extractor_core.h>
#include <gnuradio/uwb/uwb_phy_profile.h>
#include <gnuradio/uwb/uwb_demod_core.h>
#include <pmt/pmt.h>

#include <algorithm>
#include <chrono>
#include <climits>
#include <cmath>
#include <complex>
#include <cstdint>
#include <cstring>
#include <fstream>
#include <iostream>
#include <limits>
#include <memory>
#include <numeric>
#include <string>
#include <vector>

namespace {

constexpr size_t kPacketStart = 4992000;
constexpr size_t kSymbolLen = 1016;
constexpr size_t kPacketLen = 256448;
// Radar burst used inside a scheduled window: 16 SYNC + 8 SFD = 24,384
// samples, small enough to fit the default 203,776-sample window while still
// being a real code-9 preamble.
constexpr size_t kRadarBurstLen = 24 * 1016;
// Energy-gate region shape (matches production UwbDetector defaults):
// pre_trigger ring = max(user 2032, 32*D + L) = 32*100 + 1016 = 4216.
// Measured closed region length on the reference packet is 264016.
constexpr size_t kRegionPreTrigger = 4216;
constexpr size_t kRegionLenMeasured = 264016;
constexpr size_t kUserPreTrigger = 2032;
constexpr size_t kCapture = 200000;
constexpr size_t kCoarseDec = 4;
constexpr size_t kCoarseReps = 1;
constexpr size_t kCoarseMargin = 16;
constexpr size_t kCoarseStride = 1;

// Forward decls (definitions later in this TU).
bool load_packet_and_template(const std::string& cfile,
                              std::vector<gr_complex>& pkt,
                              std::vector<gr_complex>& tmpl);

// ---------------------------------------------------------------------------
// Low-overhead work-chunk instrumentation (single work thread; no atomics).
// Histogram: ≤8k | 8k–32k | 32k–128k | 128k–512k | >512k
// ---------------------------------------------------------------------------
struct WorkChunkStats {
    uint64_t work_calls = 0;
    uint64_t items_total = 0;
    int min_n = 0;
    int max_n = 0;
    uint64_t hist[5] = {};

    void record(int n)
    {
        if (n <= 0)
            return;
        ++work_calls;
        items_total += static_cast<uint64_t>(n);
        if (min_n == 0 || n < min_n)
            min_n = n;
        if (n > max_n)
            max_n = n;
        if (n <= 8192)
            ++hist[0];
        else if (n <= 32768)
            ++hist[1];
        else if (n <= 131072)
            ++hist[2];
        else if (n <= 524288)
            ++hist[3];
        else
            ++hist[4];
    }

    double mean() const
    {
        return work_calls > 0 ? static_cast<double>(items_total) /
                                    static_cast<double>(work_calls)
                              : 0.0;
    }

    void print(const char* label) const
    {
        std::printf("%s work_calls       : %llu\n", label,
                    static_cast<unsigned long long>(work_calls));
        std::printf("%s items_total      : %llu\n", label,
                    static_cast<unsigned long long>(items_total));
        std::printf("%s noutput min/mean/max : %d / %.1f / %d\n", label, min_n,
                    mean(), max_n);
        std::printf("%s hist ≤8k|8-32k|32-128k|128-512k|>512k : "
                    "%llu %llu %llu %llu %llu\n",
                    label, static_cast<unsigned long long>(hist[0]),
                    static_cast<unsigned long long>(hist[1]),
                    static_cast<unsigned long long>(hist[2]),
                    static_cast<unsigned long long>(hist[3]),
                    static_cast<unsigned long long>(hist[4]));
    }
};

// ---------------------------------------------------------------------------
// PatternSource: streams [packet, gap-silence, packet, ...] up to `target`
// samples without storing the whole stream (so ~1e9-sample runs fit in memory).
// Exact-target: never produces past d_target (fixes last-chunk overshoot).
// ---------------------------------------------------------------------------
class PatternSource : public gr::sync_block
{
public:
    PatternSource(const std::vector<gr_complex>& pkt, size_t gap, uint64_t target)
        : gr::sync_block("pattern_source",
                         gr::io_signature::make(0, 0, 0),
                         gr::io_signature::make(1, 1, sizeof(gr_complex))),
          d_pkt(pkt),
          d_gap(gap),
          d_target(target)
    {
    }

    int work(int noutput_items,
             gr_vector_const_void_star&,
             gr_vector_void_star& output_items) override
    {
        auto* out = reinterpret_cast<gr_complex*>(output_items[0]);
        int produced = 0;
        while (produced < noutput_items &&
               (d_total + static_cast<uint64_t>(produced)) < d_target) {
            const uint64_t remaining_target =
                d_target - d_total - static_cast<uint64_t>(produced);
            const size_t room = static_cast<size_t>(noutput_items - produced);
            const size_t limit =
                static_cast<size_t>(std::min<uint64_t>(remaining_target, room));
            if (limit == 0)
                break;

            if (d_cycle_pos < d_pkt.size()) {
                const size_t n =
                    std::min(d_pkt.size() - d_cycle_pos, limit);
                std::memcpy(out + produced, d_pkt.data() + d_cycle_pos,
                            n * sizeof(gr_complex));
                d_cycle_pos += n;
                produced += static_cast<int>(n);
            } else {
                const size_t gap_left = d_gap - (d_cycle_pos - d_pkt.size());
                const size_t n = std::min(gap_left, limit);
                std::memset(out + produced, 0, n * sizeof(gr_complex));
                d_cycle_pos += n;
                produced += static_cast<int>(n);
            }
            if (d_cycle_pos >= d_pkt.size() + d_gap)
                d_cycle_pos = 0;
        }
        d_total += static_cast<uint64_t>(produced);
        d_stats.record(produced);
        if (produced == 0)
            return -1; // done
        return produced;
    }

    uint64_t total_produced() const { return d_total; }
    const WorkChunkStats& stats() const { return d_stats; }

private:
    std::vector<gr_complex> d_pkt;
    size_t d_gap;
    uint64_t d_target;
    uint64_t d_total = 0;
    size_t d_cycle_pos = 0;
    WorkChunkStats d_stats;
};

// Correct GR 3.10 buffer request: max=-1 so min is honored (not min(default,max)).
void apply_source_buffer(const std::shared_ptr<gr::block>& src,
                         long buffer_items,
                         int max_noutput_items)
{
    if (buffer_items > 0) {
        src->set_max_output_buffer(0, -1);
        src->set_min_output_buffer(0, buffer_items);
    }
    if (max_noutput_items > 0)
        src->set_max_noutput_items(max_noutput_items);
}

// Build [pkt + gap silence] stream of exactly `target` samples (for vector_source).
std::vector<gr_complex> build_pattern_stream(const std::vector<gr_complex>& pkt,
                                             size_t gap,
                                             uint64_t target)
{
    std::vector<gr_complex> stream;
    stream.reserve(static_cast<size_t>(target));
    size_t cycle_pos = 0;
    while (stream.size() < target) {
        const size_t remain = static_cast<size_t>(target - stream.size());
        if (cycle_pos < pkt.size()) {
            const size_t n = std::min(pkt.size() - cycle_pos, remain);
            stream.insert(stream.end(), pkt.begin() + static_cast<std::ptrdiff_t>(cycle_pos),
                          pkt.begin() + static_cast<std::ptrdiff_t>(cycle_pos + n));
            cycle_pos += n;
        } else {
            const size_t gap_left = gap - (cycle_pos - pkt.size());
            const size_t n = std::min(gap_left, remain);
            stream.insert(stream.end(), n, gr_complex(0.0f, 0.0f));
            cycle_pos += n;
        }
        if (cycle_pos >= pkt.size() + gap)
            cycle_pos = 0;
    }
    return stream;
}

// ---------------------------------------------------------------------------
// PduCounter: message sink that counts PDUs without storing payloads.
// ---------------------------------------------------------------------------
class PduCounter : public gr::block
{
public:
    PduCounter()
        : gr::block("pdu_counter",
                    gr::io_signature::make(0, 0, 0),
                    gr::io_signature::make(0, 0, 0))
    {
        message_port_register_in(pmt::mp("packet"));
        set_msg_handler(pmt::mp("packet"), [this](pmt::pmt_t msg) {
            ++d_count;
            pmt::pmt_t data = pmt::cdr(msg);
            if (pmt::is_c32vector(data))
                d_total_samples += pmt::length(data);
        });
    }

    size_t count() const { return d_count; }
    size_t total_samples() const { return d_total_samples; }

private:
    size_t d_count = 0;
    size_t d_total_samples = 0;
};

// ---------------------------------------------------------------------------
// Energy-gate-only benchmark: runs UwbDetectorStateMachine (decimated gate +
// cross-chunk region buffering) directly, WITHOUT the coarse/fine preamble
// confirmation and PDU emission.  This isolates the energy-gate stage cost.
// ---------------------------------------------------------------------------
int run_gate_bench(const std::string& cfile, uint64_t target, size_t gap)
{
    std::ifstream f(cfile, std::ios::binary);
    if (!f) {
        std::cerr << "cannot open " << cfile << "\n";
        return 1;
    }
    f.seekg(0, std::ios::end);
    size_t bytes = static_cast<size_t>(f.tellg());
    f.seekg(0, std::ios::beg);
    std::vector<gr_complex> base(bytes / sizeof(gr_complex));
    f.read(reinterpret_cast<char*>(base.data()), static_cast<std::streamsize>(bytes));

    std::vector<gr_complex> pkt(base.begin() + kPacketStart,
                                base.begin() + kPacketStart + kPacketLen);

    // Sparse stream [packet + gap] in memory, matching the full-detector bench.
    std::vector<gr_complex> stream;
    stream.reserve(static_cast<size_t>(target));
    while (stream.size() < target) {
        const size_t remain = static_cast<size_t>(target - stream.size());
        const size_t pkt_n = std::min(pkt.size(), remain);
        stream.insert(stream.end(), pkt.begin(), pkt.begin() + pkt_n);
        if (stream.size() >= target)
            break;
        const size_t gap_n = std::min(gap, remain - pkt_n);
        stream.insert(stream.end(), gap_n, gr_complex(0.0f, 0.0f));
    }

    gr::uwb::UwbDetectorStateMachine sm(4216, 1e-3f, 100, 32, 8);
    size_t regions = 0;
    uint64_t total_region_iq = 0;

    const auto w0 = std::chrono::steady_clock::now();
    const auto c0 = std::clock();
    const size_t CH = 8192;
    for (size_t off = 0; off < stream.size(); off += CH) {
        const size_t n = std::min(CH, stream.size() - off);
        sm.process(stream.data() + off, n, off);
        while (sm.region_ready()) {
            const auto handle = sm.take_region();
            ++regions;
            total_region_iq += sm.region(handle).samples.size();
            sm.release_region(handle);
        }
    }
    const auto c1 = std::clock();
    const auto w1 = std::chrono::steady_clock::now();

    const double wall = std::chrono::duration<double>(w1 - w0).count();
    const double cpu = static_cast<double>(c1 - c0) / CLOCKS_PER_SEC;
    const double n = static_cast<double>(stream.size());

    std::printf("== Energy gate (state machine only, no coarse/fine) ==\n");
    std::printf("processed samples : %zu\n", stream.size());
    std::printf("elapsed time      : %.3f s\n", wall);
    std::printf("CPU time          : %.3f s  (utilization %.0f%%)\n", cpu,
                wall > 0 ? 100.0 * cpu / wall : 0.0);
    std::printf("throughput        : %.1f MS/s\n", n / wall / 1e6);
    std::printf("throughput        : %.2f GB/s\n", n * 8.0 / wall / 1e9);
    // Output of the energy-gate stage is a sparse stream of Region objects
    // (pre-trigger + raw IQ above the threshold).  Report the average IQ rate
    // of those Region payloads, their mean length, and how much of the input
    // is retained as region IQ.
    std::printf("regions           : %zu\n", regions);
    std::printf("region IQ total   : %llu samples\n",
                static_cast<unsigned long long>(total_region_iq));
    std::printf("avg region length : %.0f samples\n",
                regions > 0 ? static_cast<double>(total_region_iq) / regions : 0.0);
    std::printf("region IQ rate    : %.3f MS/s (%.2f GB/s)\n",
                total_region_iq / wall / 1e6, total_region_iq * 8.0 / wall / 1e9);
    std::printf("capture fraction  : %.3f%% of input\n",
                100.0 * total_region_iq / n);
    std::printf("\n");
    return 0;
}

// Layered GR modes share PatternSource (default), target, gap, buffer config.
//   source-null     : PatternSource → null_sink
//   source-search   : PatternSource → UwbDetector (threshold never fires)
//   detector-sparse : PatternSource → UwbDetector → PduCounter (normal thr)
//   detector-dense  : same as sparse with small gap
struct LayeredBenchConfig {
    std::string mode; // source-null | source-search | detector-sparse | detector-dense
    uint64_t target = 500000000ULL;
    size_t gap = 998143552; // ~1 pkt/s at 1 GS/s scale (scheme primary plumbing gap)
    long buffer_items = 0;  // 0 = GR default
    int max_noutput_items = 0; // 0 = leave defaults (detector keeps 65536)
    std::string source_type = "pattern"; // pattern | vector
    float energy_threshold = 1e-3f; // source-search overrides to never-fire unless set
    bool threshold_override = false;
    int repeat = 1;
};

struct LayeredRunResult {
    double wall_s = 0;
    double cpu_s = 0;
    double ms_per_s = 0;
    double gb_per_s = 0;
    uint64_t processed = 0;
    size_t detections = 0;
    uint64_t dropped = 0;
    uint64_t captured_iq = 0;
    WorkChunkStats src_stats;
    WorkChunkStats det_stats;
    bool has_det_stats = false;
};

int run_layered_once(const std::string& cfile,
                     const LayeredBenchConfig& cfg,
                     LayeredRunResult& out)
{
    std::vector<gr_complex> pkt, tmpl;
    if (!load_packet_and_template(cfile, pkt, tmpl))
        return 1;

    const bool is_null = (cfg.mode == "source-null");
    const bool is_search = (cfg.mode == "source-search");
    const bool is_detect = (cfg.mode == "detector-sparse" ||
                            cfg.mode == "detector-dense" ||
                            cfg.mode == "detector" ||
                            cfg.mode == "detector-region-stream");

    float thr = cfg.energy_threshold;
    if (is_search && !cfg.threshold_override)
        thr = 1e30f; // never leave SEARCH / never close a region

    size_t gap = cfg.gap;
    if (cfg.mode == "detector-dense" && gap == 0)
        gap = 6000;

    // --- source construction (stream build time NOT in wall clock for vector) ---
    std::shared_ptr<gr::block> src_blk;
    std::shared_ptr<PatternSource> pattern_src;
    std::vector<gr_complex> vector_stream; // keep alive for vector_source

    if (cfg.source_type == "vector") {
        if (cfg.target > 250000000ULL) {
            std::cerr << "vector source refused for target > 250M "
                         "(use --source pattern or smaller --target)\n";
            return 1;
        }
        vector_stream = build_pattern_stream(pkt, gap, cfg.target);
        auto vs = gr::blocks::vector_source_c::make(vector_stream);
        apply_source_buffer(vs, cfg.buffer_items, cfg.max_noutput_items);
        src_blk = vs;
    } else {
        pattern_src = std::make_shared<PatternSource>(pkt, gap, cfg.target);
        apply_source_buffer(pattern_src, cfg.buffer_items, cfg.max_noutput_items);
        src_blk = pattern_src;
    }

    auto tb = gr::make_top_block("bench_layered");
    if (cfg.max_noutput_items > 0)
        tb->set_max_noutput_items(cfg.max_noutput_items);

    gr::uwb::UwbDetector::sptr det;
    std::shared_ptr<PduCounter> cnt;

    if (is_null) {
        auto ns = gr::blocks::null_sink::make(sizeof(gr_complex));
        tb->connect(src_blk, 0, ns, 0);
    } else if (is_search || is_detect) {
        det = gr::uwb::UwbDetector::make(tmpl, 2032, 200000, thr, 100, 4, 1, 16);
        det->reset_work_stats();
        // Allow large work chunks when scanning buffers (constructor caps at 65536).
        if (cfg.max_noutput_items > 0)
            det->set_max_noutput_items(cfg.max_noutput_items);
        else if (cfg.buffer_items > 65536)
            det->unset_max_noutput_items();
        tb->connect(src_blk, 0, det, 0);
        if (is_detect) {
            cnt = std::make_shared<PduCounter>();
            tb->msg_connect(det, "packet", cnt, "packet");
        }
    } else {
        std::cerr << "unknown layered mode: " << cfg.mode << "\n";
        return 1;
    }

    const auto w0 = std::chrono::steady_clock::now();
    const auto c0 = std::clock();
    tb->run();
    const auto c1 = std::clock();
    const auto w1 = std::chrono::steady_clock::now();

    out.wall_s = std::chrono::duration<double>(w1 - w0).count();
    out.cpu_s = static_cast<double>(c1 - c0) / CLOCKS_PER_SEC;

    if (pattern_src) {
        out.processed = pattern_src->total_produced();
        out.src_stats = pattern_src->stats();
    } else {
        out.processed = cfg.target; // vector_source drains exactly the vector
        out.src_stats = WorkChunkStats{}; // not instrumented
    }

    if (out.processed != cfg.target) {
        std::cerr << "WARNING: processed (" << out.processed
                  << ") != target (" << cfg.target << ")\n";
    }

    const double n = static_cast<double>(out.processed);
    out.ms_per_s = out.wall_s > 0 ? n / out.wall_s / 1e6 : 0.0;
    out.gb_per_s = out.wall_s > 0 ? n * 8.0 / out.wall_s / 1e9 : 0.0;

    if (det) {
        out.has_det_stats = true;
        out.det_stats.work_calls = det->work_calls();
        out.det_stats.items_total = det->work_items_total();
        out.det_stats.min_n = det->work_min_noutput_items();
        out.det_stats.max_n = det->work_max_noutput_items();
        det->work_noutput_histogram(out.det_stats.hist);
        out.dropped = det->dropped_regions();
    }
    if (cnt) {
        out.detections = cnt->count();
        out.captured_iq = cnt->total_samples();
    }
    return 0;
}

void print_layered_result(const LayeredBenchConfig& cfg, const LayeredRunResult& r)
{
    std::printf("== layered %s ==\n", cfg.mode.c_str());
    std::printf("source            : %s\n", cfg.source_type.c_str());
    std::printf("target            : %llu\n",
                static_cast<unsigned long long>(cfg.target));
    std::printf("gap               : %zu\n", cfg.gap);
    std::printf("buffer_items      : %ld  max_noutput_items : %d\n",
                cfg.buffer_items, cfg.max_noutput_items);
    std::printf("threshold         : %g\n",
                (cfg.mode == "source-search" && !cfg.threshold_override)
                    ? 1e30
                    : static_cast<double>(cfg.energy_threshold));
    std::printf("processed samples : %llu  (target %llu)\n",
                static_cast<unsigned long long>(r.processed),
                static_cast<unsigned long long>(cfg.target));
    std::printf("elapsed time      : %.3f s\n", r.wall_s);
    std::printf("CPU time          : %.3f s  (utilization %.0f%%)\n", r.cpu_s,
                r.wall_s > 0 ? 100.0 * r.cpu_s / r.wall_s : 0.0);
    std::printf("throughput        : %.1f MS/s\n", r.ms_per_s);
    std::printf("throughput        : %.2f GB/s\n", r.gb_per_s);
    if (cfg.source_type == "pattern")
        r.src_stats.print("src");
    if (r.has_det_stats)
        r.det_stats.print("det");
    std::printf("dropped regions   : %llu\n",
                static_cast<unsigned long long>(r.dropped));
    std::printf("detections        : %zu\n", r.detections);
    if (r.captured_iq > 0) {
        const double n = static_cast<double>(r.processed);
        std::printf("captured IQ       : %llu samples (%.2f%% of input)\n",
                    static_cast<unsigned long long>(r.captured_iq),
                    n > 0 ? 100.0 * r.captured_iq / n : 0.0);
        std::printf("output stream     : %.3f MS/s (%.2f GB/s)\n",
                    r.wall_s > 0 ? r.captured_iq / r.wall_s / 1e6 : 0.0,
                    r.wall_s > 0 ? r.captured_iq * 8.0 / r.wall_s / 1e9 : 0.0);
    }
    std::printf("1 GS/s target     : %s\n",
                r.ms_per_s >= 1000.0 ? "MET" : "not yet");
    std::printf("\n");
}

int run_layered_bench(const std::string& cfile, LayeredBenchConfig cfg)
{
    if (cfg.mode == "detector-dense" && cfg.gap == 998143552)
        cfg.gap = 6000; // dense default unless user overrode gap earlier... handled in main

    std::vector<double> rates;
    rates.reserve(static_cast<size_t>(cfg.repeat));
    LayeredRunResult last{};

    for (int rep = 0; rep < cfg.repeat; ++rep) {
        LayeredRunResult r{};
        if (run_layered_once(cfile, cfg, r) != 0)
            return 1;
        if (cfg.repeat > 1)
            std::printf("--- repeat %d/%d ---\n", rep + 1, cfg.repeat);
        print_layered_result(cfg, r);
        rates.push_back(r.ms_per_s);
        last = r;
    }

    if (cfg.repeat > 1 && !rates.empty()) {
        std::sort(rates.begin(), rates.end());
        const double med = rates[rates.size() / 2];
        std::printf("== summary %s  n=%d  min=%.1f med=%.1f max=%.1f MS/s ==\n\n",
                    cfg.mode.c_str(), cfg.repeat, rates.front(), med, rates.back());
    }
    (void)last;
    return 0;
}

// Backward-compatible wrapper used by detector-region-stream path.
int run_detector_bench(const std::string& cfile,
                       uint64_t target,
                       size_t gap,
                       const std::string& label)
{
    LayeredBenchConfig cfg;
    cfg.mode = "detector-sparse";
    cfg.target = target;
    cfg.gap = gap;
    cfg.buffer_items = 0;
    cfg.max_noutput_items = 0;
    cfg.source_type = "pattern";
    cfg.energy_threshold = 1e-3f;
    cfg.repeat = 1;
    (void)label;
    return run_layered_bench(cfile, cfg);
}

// ---------------------------------------------------------------------------
// Load cfile packet + 1016-sample template.  Returns false on I/O error.
// ---------------------------------------------------------------------------
bool load_packet_and_template(const std::string& cfile,
                              std::vector<gr_complex>& pkt,
                              std::vector<gr_complex>& tmpl)
{
    std::ifstream f(cfile, std::ios::binary);
    if (!f) {
        std::cerr << "cannot open " << cfile << "\n";
        return false;
    }
    f.seekg(0, std::ios::end);
    size_t bytes = static_cast<size_t>(f.tellg());
    f.seekg(0, std::ios::beg);
    std::vector<gr_complex> base(bytes / sizeof(gr_complex));
    f.read(reinterpret_cast<char*>(base.data()), static_cast<std::streamsize>(bytes));
    if (base.size() < kPacketStart + kPacketLen) {
        std::cerr << "cfile too short for packet slice\n";
        return false;
    }
    tmpl.assign(base.begin() + kPacketStart,
                base.begin() + kPacketStart + kSymbolLen);
    pkt.assign(base.begin() + kPacketStart,
               base.begin() + kPacketStart + kPacketLen);
    return true;
}

// Build a production-shaped Region IQ buffer: pre-trigger zeros + packet +
// trailing zeros so the total length matches the energy-gate measurement
// (264016).  This is the object the worker would receive after a gate close.
std::vector<gr_complex> build_region_iq(const std::vector<gr_complex>& pkt)
{
    std::vector<gr_complex> region(kRegionLenMeasured, gr_complex(0.0f, 0.0f));
    const size_t n = std::min(pkt.size(), kRegionLenMeasured - kRegionPreTrigger);
    std::memcpy(region.data() + kRegionPreTrigger, pkt.data(),
                n * sizeof(gr_complex));
    return region;
}

// Production-equivalent coarse scan end: preamble-horizon prefix (P0).
// candidate_offset=0 here (synthetic region); matches publish_packet when
// candidate_offset is 0 or when horizon already covers pre+SYNC+SFD.
inline size_t production_coarse_scan_end(size_t region_len,
                                         size_t template_len,
                                         size_t candidate_offset = 0)
{
    const size_t preamble_span =
        template_len * (gr::uwb::core::kUwbSyncSymbols +
                        gr::uwb::core::kUwbSfdSymbols + 4);
    const size_t horizon = std::max(preamble_span, candidate_offset + preamble_span);
    return std::min(region_len, horizon);
}

// Run the same coarse → fine → capture path as UwbDetector::publish_packet
// (P0 preamble-horizon + P1 first-peak fine).  Returns true if a preamble
// was confirmed (would emit a PDU).
bool process_region_like_detector(
    const std::vector<gr_complex>& region,
    const std::vector<gr_complex>& tmpl_ds,
    size_t sym_ds,
    gr::filter::kernel::fir_filter_ccc& fir,
    float template_energy,
    size_t template_len,
    size_t pre_trigger,
    size_t capture,
    // scratch (reused across calls)
    std::vector<gr_complex>& sig_ds,
    std::vector<float>& pow_ds,
    std::vector<float>& score_ds,
    std::vector<float>& metric_ds,
    std::vector<size_t>& coarse_peaks,
    std::vector<gr_complex>& corr,
    std::vector<float>& winpow,
    std::vector<float>& fine_metric,
    size_t& out_capture_len)
{
    out_capture_len = 0;
    const size_t n = region.size();
    if (n == 0)
        return false;

    const size_t coarse_scan_end =
        production_coarse_scan_end(n, template_len, /*candidate_offset=*/0);

    float mx = 0.0f;
    gr::uwb::core::uwb_coarse_peaks(region.data(),
                                    0,
                                    coarse_scan_end,
                                    tmpl_ds.data(),
                                    tmpl_ds.size(),
                                    kCoarseDec,
                                    kCoarseReps,
                                    sym_ds,
                                    /*peak_rel=*/0.5f,
                                    /*exist_frac=*/0.5f,
                                    kCoarseStride,
                                    sig_ds,
                                    pow_ds,
                                    score_ds,
                                    metric_ds,
                                    coarse_peaks,
                                    &mx);
    if (coarse_peaks.empty())
        return false;

    const size_t Lm1 = template_len - 1;
    const size_t half = kCoarseStride * kCoarseDec + kCoarseMargin;
    size_t earliest_end = n;
    float best_metric = 0.0f;
    for (size_t p : coarse_peaks) {
        if (p + Lm1 >= n)
            continue;
        const size_t center = p + Lm1;
        const size_t j0 = (center > half) ? center - half : 0;
        const size_t j1 = std::min(center + half, n - 1);
        const size_t len = j1 - j0 + 1;

        fir.filterN(corr.data(), region.data() + j0,
                    static_cast<unsigned long>(len));
        gr::uwb::core::uwb_window_power(region.data() + j0, len, template_len,
                                        winpow.data());
        gr::uwb::core::uwb_normalized_score(corr.data(), winpow.data(), len,
                                            template_energy, fine_metric.data());

        size_t local_best = 0;
        for (size_t k = 1; k < len; ++k) {
            if (fine_metric[k] > fine_metric[local_best])
                local_best = k;
        }
        // P1: first fine-confirmed peak (ascending) is the earliest start.
        if (fine_metric[local_best] >= 0.5f) {
            earliest_end = j0 + local_best;
            best_metric = fine_metric[local_best];
            break;
        }
    }
    if (earliest_end >= n || best_metric < 0.5f)
        return false;

    // Capture clamp matches publish_packet (region-relative, start at 0).
    // packet_start_rel = earliest_end - (L-1); lo_off from pre_trigger.
    const size_t packet_start_rel = earliest_end - Lm1;
    const size_t lo_off =
        (packet_start_rel >= pre_trigger) ? packet_start_rel - pre_trigger : 0;
    out_capture_len = std::min(pre_trigger + capture, n - lo_off);

    // Materialize capture IQ the way PDU construction does (memcpy cost).
    std::vector<gr_complex> iq(region.begin() + static_cast<std::ptrdiff_t>(lo_off),
                               region.begin() + static_cast<std::ptrdiff_t>(lo_off +
                                                                            out_capture_len));
    (void)iq;
    return true;
}

// ---------------------------------------------------------------------------
// Direct continuous Region-block stress test.
// Feeds the same Region IQ repeatedly into the production coarse/fine path
// (no energy gate, no GNU Radio scheduler).  Reports the maximum Region IQ
// sample rate the detector worker algorithm can sustain.
// ---------------------------------------------------------------------------
int run_region_core_bench(const std::string& cfile, size_t n_regions)
{
    std::vector<gr_complex> pkt, tmpl;
    if (!load_packet_and_template(cfile, pkt, tmpl))
        return 1;

    const std::vector<gr_complex> region = build_region_iq(pkt);

    // Matched-filter taps: reversed conjugated L2-normalized template.
    std::vector<gr_complex> tmpl_norm = tmpl;
    gr::uwb::core::uwb_l2_normalize(tmpl_norm);
    const float tenergy = gr::uwb::core::uwb_template_energy(tmpl_norm);
    std::vector<gr_complex> taps;
    taps.reserve(tmpl_norm.size());
    for (auto it = tmpl_norm.rbegin(); it != tmpl_norm.rend(); ++it)
        taps.push_back(std::conj(*it));
    gr::filter::kernel::fir_filter_ccc fir(taps);

    // Decimated template (same as UwbDetector::rebuild_decimated_template).
    const size_t D = kCoarseDec;
    const size_t Ld = tmpl.size() / D;
    std::vector<gr_complex> tmpl_ds;
    tmpl_ds.reserve(Ld);
    for (size_t j = 0; j < Ld; ++j) {
        const size_t m = j * D;
        tmpl_ds.push_back(std::conj(taps[tmpl.size() - 1 - m]));
    }
    gr::uwb::core::uwb_l2_normalize(tmpl_ds);
    const size_t sym_ds = Ld;

    // Scratch (pre-sized like the worker).
    std::vector<gr_complex> sig_ds, corr;
    std::vector<float> pow_ds, score_ds, metric_ds, winpow, fine_metric;
    std::vector<size_t> coarse_peaks;
    const size_t half = kCoarseStride * kCoarseDec + kCoarseMargin;
    corr.resize(2 * half + 1);
    winpow.resize(2 * half + 1);
    fine_metric.resize(2 * half + 1);
    coarse_peaks.reserve(256);

    // Warm-up (one region) so VOLK dispatch / cache is settled.
    size_t cap0 = 0;
    const bool warm_ok = process_region_like_detector(
        region, tmpl_ds, sym_ds, fir, tenergy, tmpl.size(), kUserPreTrigger,
        kCapture, sig_ds, pow_ds, score_ds, metric_ds, coarse_peaks, corr,
        winpow, fine_metric, cap0);
    if (!warm_ok) {
        std::cerr << "region core warm-up failed to detect preamble "
                     "(check region construction)\n";
        return 1;
    }

    size_t detections = 0;
    uint64_t total_capture = 0;
    const uint64_t total_region_iq =
        static_cast<uint64_t>(n_regions) * region.size();

    const auto w0 = std::chrono::steady_clock::now();
    const auto c0 = std::clock();
    for (size_t i = 0; i < n_regions; ++i) {
        size_t cap = 0;
        if (process_region_like_detector(region, tmpl_ds, sym_ds, fir, tenergy,
                                         tmpl.size(), kUserPreTrigger, kCapture,
                                         sig_ds, pow_ds, score_ds, metric_ds,
                                         coarse_peaks, corr, winpow, fine_metric,
                                         cap)) {
            ++detections;
            total_capture += cap;
        }
    }
    const auto c1 = std::clock();
    const auto w1 = std::chrono::steady_clock::now();

    const double wall = std::chrono::duration<double>(w1 - w0).count();
    const double cpu = static_cast<double>(c1 - c0) / CLOCKS_PER_SEC;
    const double region_ms =
        wall / static_cast<double>(n_regions) * 1e3;

    std::printf("== UwbDetector Region core (continuous Region blocks) ==\n");
    std::printf("region length     : %zu samples (pre=%zu + packet + tail)\n",
                region.size(), kRegionPreTrigger);
    std::printf("regions processed : %zu\n", n_regions);
    std::printf("detections        : %zu\n", detections);
    std::printf("region IQ total   : %llu samples\n",
                static_cast<unsigned long long>(total_region_iq));
    std::printf("elapsed time      : %.3f s\n", wall);
    std::printf("CPU time          : %.3f s  (utilization %.0f%%)\n", cpu,
                wall > 0 ? 100.0 * cpu / wall : 0.0);
    std::printf("time per region   : %.3f ms\n", region_ms);
    std::printf("regions / s       : %.1f\n",
                wall > 0 ? n_regions / wall : 0.0);
    // Primary metric: max Region IQ sample rate the coarse/fine path sustains.
    std::printf("region IQ rate    : %.3f MS/s (%.2f GB/s)\n",
                total_region_iq / wall / 1e6, total_region_iq * 8.0 / wall / 1e9);
    std::printf("capture IQ total  : %llu samples\n",
                static_cast<unsigned long long>(total_capture));
    std::printf("capture IQ rate   : %.3f MS/s\n",
                total_capture / wall / 1e6);
    // How many real-time 1 GS/s packets this rate supports (region ≈ 264k).
    const double max_pkt_per_s =
        wall > 0 ? static_cast<double>(n_regions) / wall : 0.0;
    std::printf("equiv packet rate : %.1f pkt/s @ region=%zu\n", max_pkt_per_s,
                region.size());
    std::printf("vs gate@1pkt/s    : gate region IQ ~1.18 MS/s; "
                "core capacity / gate load = %.0fx\n",
                (total_region_iq / wall / 1e6) / 1.18);
    std::printf("\n");
    return 0;
}

// ---------------------------------------------------------------------------
// Full UwbDetector under continuous region pressure: packet + min gap so the
// energy gate keeps closing regions as fast as possible.  Measures drops and
// effective region/capture rates through the real worker + job queue.
// ---------------------------------------------------------------------------
int run_region_stream_bench(const std::string& cfile,
                            uint64_t target,
                            size_t gap)
{
    // gap=6000 is the existing detector-dense default (above ~4000 min holdoff).
    if (gap == 0)
        gap = 6000;
    return run_detector_bench(cfile, target, gap,
                              "detector-region-stream (gap=" +
                                  std::to_string(gap) + ")");
}

// ---------------------------------------------------------------------------
// Component-level profiler: times each detector stage in isolation so we can
// rank optimization targets.  All numbers are wall-clock (steady_clock).
// ---------------------------------------------------------------------------
using Clock = std::chrono::steady_clock;
inline double secs(Clock::time_point a, Clock::time_point b)
{
    return std::chrono::duration<double>(b - a).count();
}

int run_detector_profile(const std::string& cfile, size_t n_regions)
{
    std::vector<gr_complex> pkt, tmpl;
    if (!load_packet_and_template(cfile, pkt, tmpl))
        return 1;

    const std::vector<gr_complex> region = build_region_iq(pkt);
    const size_t Rn = region.size();

    // ---- template / FIR setup (same as production) ----
    std::vector<gr_complex> tmpl_norm = tmpl;
    gr::uwb::core::uwb_l2_normalize(tmpl_norm);
    const float tenergy = gr::uwb::core::uwb_template_energy(tmpl_norm);
    std::vector<gr_complex> taps;
    taps.reserve(tmpl_norm.size());
    for (auto it = tmpl_norm.rbegin(); it != tmpl_norm.rend(); ++it)
        taps.push_back(std::conj(*it));
    gr::filter::kernel::fir_filter_ccc fir(taps);

    auto make_tmpl_ds = [&](size_t D) {
        const size_t Ld = tmpl.size() / D;
        std::vector<gr_complex> ds;
        ds.reserve(Ld);
        for (size_t j = 0; j < Ld; ++j) {
            const size_t m = j * D;
            ds.push_back(std::conj(taps[tmpl.size() - 1 - m]));
        }
        gr::uwb::core::uwb_l2_normalize(ds);
        return ds;
    };
    auto tmpl_ds4 = make_tmpl_ds(4);
    auto tmpl_ds8 = make_tmpl_ds(8);

    std::vector<gr_complex> sig_ds, corr;
    std::vector<float> pow_ds, score_ds, metric_ds, winpow, fine_metric;
    std::vector<size_t> coarse_peaks;
    const size_t half = kCoarseStride * kCoarseDec + kCoarseMargin;
    corr.resize(2 * half + 1);
    winpow.resize(2 * half + 1);
    fine_metric.resize(2 * half + 1);
    coarse_peaks.reserve(256);

    // Warm-up
    {
        float mx = 0.0f;
        gr::uwb::core::uwb_coarse_peaks(region.data(), 0, Rn, tmpl_ds4.data(),
                                        tmpl_ds4.size(), 4, 1, tmpl_ds4.size(),
                                        0.5f, 0.5f, 1, sig_ds, pow_ds, score_ds,
                                        metric_ds, coarse_peaks, &mx);
    }

    const size_t N = n_regions > 0 ? n_regions : 200;
    std::printf("== Detector component profile (N=%zu regions, len=%zu) ==\n",
                N, Rn);
    std::printf("CPU: profiled on this host; times are wall-clock means.\n\n");

    // =====================================================================
    // A. Energy-gate state machine: SEARCH (silence) vs IN_REGION (packet)
    // =====================================================================
    {
        constexpr size_t kSilence = 50'000'000; // 50 M silence
        std::vector<gr_complex> silence(1 << 16, gr_complex(0.0f, 0.0f)); // chunk
        gr::uwb::UwbDetectorStateMachine sm_s(kRegionPreTrigger, 1e-3f, 100, 32, 8);
        auto t0 = Clock::now();
        uint64_t abs = 0;
        size_t left = kSilence;
        while (left > 0) {
            const size_t n = std::min(left, silence.size());
            sm_s.process(silence.data(), n, abs);
            abs += n;
            left -= n;
            while (sm_s.region_ready()) {
                auto h = sm_s.take_region();
                sm_s.release_region(h);
            }
        }
        auto t1 = Clock::now();
        const double s_search = secs(t0, t1);
        std::printf("--- A. Energy gate state machine ---\n");
        std::printf("SEARCH silence  : %zu samples in %.3f s  → %.1f MS/s  "
                    "(%.2f ns/sample)\n",
                    kSilence, s_search, kSilence / s_search / 1e6,
                    s_search * 1e9 / kSilence);
    }
    {
        // Dense packet stream: almost all samples go through IN_REGION.
        constexpr size_t kTarget = 20'000'000;
        constexpr size_t kGap = 6000;
        std::vector<gr_complex> stream;
        stream.reserve(kTarget);
        while (stream.size() < kTarget) {
            const size_t remain = kTarget - stream.size();
            const size_t pn = std::min(pkt.size(), remain);
            stream.insert(stream.end(), pkt.begin(), pkt.begin() + pn);
            if (stream.size() >= kTarget)
                break;
            const size_t gn = std::min(kGap, remain - pn);
            stream.insert(stream.end(), gn, gr_complex(0.0f, 0.0f));
        }
        gr::uwb::UwbDetectorStateMachine sm(kRegionPreTrigger, 1e-3f, 100, 32, 8);
        size_t regions = 0;
        uint64_t region_iq = 0;
        auto t0 = Clock::now();
        const size_t CH = 8192;
        for (size_t off = 0; off < stream.size(); off += CH) {
            const size_t n = std::min(CH, stream.size() - off);
            sm.process(stream.data() + off, n, off);
            while (sm.region_ready()) {
                auto h = sm.take_region();
                region_iq += sm.region(h).samples.size();
                sm.release_region(h);
                ++regions;
            }
        }
        sm.flush_region();
        while (sm.region_ready()) {
            auto h = sm.take_region();
            region_iq += sm.region(h).samples.size();
            sm.release_region(h);
            ++regions;
        }
        auto t1 = Clock::now();
        const double s = secs(t0, t1);
        std::printf("IN_REGION dense : %zu samples in %.3f s  → %.1f MS/s  "
                    "(%.2f ns/sample)\n",
                    stream.size(), s, stream.size() / s / 1e6,
                    s * 1e9 / stream.size());
        std::printf("  regions=%zu  region IQ=%llu  materialize ≈ %.1f MS/s\n",
                    regions, static_cast<unsigned long long>(region_iq),
                    region_iq / s / 1e6);
        std::printf("  (IN_REGION cost dominated by preallocated-pool memcpy of "
                    "raw IQ into Region)\n\n");
    }

    // =====================================================================
    // B. Coarse sub-stages on one production-sized Region
    // =====================================================================
    {
        const size_t D = 4;
        const size_t Ld = tmpl_ds4.size();
        const size_t nd = Rn / D;
        const size_t nscore = nd - Ld + 1;
        double t_decim = 0, t_pow = 0, t_score = 0, t_peak = 0, t_total = 0;
        size_t n_peaks = 0;

        for (size_t i = 0; i < N; ++i) {
            auto a0 = Clock::now();
            sig_ds.resize(nd);
            for (size_t j = 0; j < nd; ++j)
                sig_ds[j] = region[j * D];
            auto a1 = Clock::now();

            pow_ds.resize(nd);
            float etd = 0.0f;
            for (size_t k = 0; k < Ld; ++k)
                etd += std::norm(tmpl_ds4[k]);
            float psum = 0.0f;
            for (size_t k = 0; k < Ld; ++k)
                psum += std::norm(sig_ds[k]);
            pow_ds[0] = psum;
            for (size_t j = 1; j + Ld <= nd; ++j) {
                psum += std::norm(sig_ds[j + Ld - 1]) - std::norm(sig_ds[j - 1]);
                pow_ds[j] = psum;
            }
            auto a2 = Clock::now();

            score_ds.resize(nscore);
            for (size_t j = 0; j < nscore; ++j) {
                std::complex<float> acc(0.0f, 0.0f);
                volk_32fc_x2_conjugate_dot_prod_32fc(
                    &acc, sig_ds.data() + j, tmpl_ds4.data(),
                    static_cast<unsigned int>(Ld));
                score_ds[j] =
                    std::abs(acc) /
                    (std::sqrt(pow_ds[j] * etd + gr::uwb::core::kUwbEpsilon));
            }
            auto a3 = Clock::now();

            // peak pick (R=1 metric = score)
            float mx = 0.0f;
            for (size_t j = 0; j < nscore; ++j)
                if (score_ds[j] > mx)
                    mx = score_ds[j];
            coarse_peaks.clear();
            const float thr = 0.5f * mx;
            for (size_t j = 1; j + 1 < nscore; ++j) {
                if (score_ds[j] > thr && score_ds[j] > score_ds[j - 1] &&
                    score_ds[j] >= score_ds[j + 1]) {
                    if (coarse_peaks.empty() ||
                        j >= coarse_peaks.back() + Ld / 2)
                        coarse_peaks.push_back(j);
                    else if (j > coarse_peaks.back())
                        coarse_peaks.back() = j;
                }
            }
            n_peaks = coarse_peaks.size();
            auto a4 = Clock::now();

            t_decim += secs(a0, a1);
            t_pow += secs(a1, a2);
            t_score += secs(a2, a3);
            t_peak += secs(a3, a4);
            t_total += secs(a0, a4);
        }

        // Also full uwb_coarse_peaks for cross-check
        double t_api = 0;
        for (size_t i = 0; i < N; ++i) {
            float mx = 0.0f;
            auto a0 = Clock::now();
            gr::uwb::core::uwb_coarse_peaks(
                region.data(), 0, Rn, tmpl_ds4.data(), tmpl_ds4.size(), 4, 1,
                tmpl_ds4.size(), 0.5f, 0.5f, 1, sig_ds, pow_ds, score_ds,
                metric_ds, coarse_peaks, &mx);
            auto a1 = Clock::now();
            t_api += secs(a0, a1);
        }

        // Coarse with D=8 for comparison
        double t_d8 = 0;
        auto tmpl8 = tmpl_ds8;
        for (size_t i = 0; i < N; ++i) {
            float mx = 0.0f;
            auto a0 = Clock::now();
            gr::uwb::core::uwb_coarse_peaks(
                region.data(), 0, Rn, tmpl8.data(), tmpl8.size(), 8, 1,
                tmpl8.size(), 0.5f, 0.5f, 1, sig_ds, pow_ds, score_ds, metric_ds,
                coarse_peaks, &mx);
            auto a1 = Clock::now();
            t_d8 += secs(a0, a1);
        }

        std::printf("--- B. Coarse preamble scan (per region, avg of %zu) ---\n",
                    N);
        std::printf("  decimate D=4      : %7.3f ms  (%5.1f%%)\n",
                    t_decim / N * 1e3, 100.0 * t_decim / t_total);
        std::printf("  window power      : %7.3f ms  (%5.1f%%)\n",
                    t_pow / N * 1e3, 100.0 * t_pow / t_total);
        std::printf("  VOLK score loop   : %7.3f ms  (%5.1f%%)  "
                    "[nscore=%zu, Ld=%zu]\n",
                    t_score / N * 1e3, 100.0 * t_score / t_total, nscore, Ld);
        std::printf("  peak pick         : %7.3f ms  (%5.1f%%)  "
                    "[~%zu peaks]\n",
                    t_peak / N * 1e3, 100.0 * t_peak / t_total, n_peaks);
        std::printf("  SUM instrumented  : %7.3f ms\n", t_total / N * 1e3);
        std::printf("  uwb_coarse_peaks  : %7.3f ms  (API, D=4)\n",
                    t_api / N * 1e3);
        std::printf("  uwb_coarse_peaks  : %7.3f ms  (API, D=8, ~%.1fx)\n",
                    t_d8 / N * 1e3, t_api / t_d8);
        std::printf("  cost model        : O((N/D)*(L/D)) = %zu·%zu = %zu "
                    "dot-products @ Ld=%zu\n\n",
                    nscore, Ld, nscore * Ld, Ld);
    }

    // =====================================================================
    // C. Fine ROI correlation (per coarse peak + total)
    // =====================================================================
    {
        // Get real coarse peaks once
        float mx = 0.0f;
        gr::uwb::core::uwb_coarse_peaks(
            region.data(), 0, Rn, tmpl_ds4.data(), tmpl_ds4.size(), 4, 1,
            tmpl_ds4.size(), 0.5f, 0.5f, 1, sig_ds, pow_ds, score_ds, metric_ds,
            coarse_peaks, &mx);
        const auto peaks = coarse_peaks;
        const size_t Lm1 = tmpl.size() - 1;
        const size_t halfw = kCoarseStride * kCoarseDec + kCoarseMargin;

        double t_fir = 0, t_wpow = 0, t_nscore = 0, t_argmax = 0, t_all = 0;
        size_t fine_calls = 0;
        size_t total_len = 0;

        for (size_t i = 0; i < N; ++i) {
            auto b0 = Clock::now();
            for (size_t p : peaks) {
                if (p + Lm1 >= Rn)
                    continue;
                const size_t center = p + Lm1;
                const size_t j0 = (center > halfw) ? center - halfw : 0;
                const size_t j1 = std::min(center + halfw, Rn - 1);
                const size_t len = j1 - j0 + 1;
                total_len += len;
                ++fine_calls;

                auto c0 = Clock::now();
                fir.filterN(corr.data(), region.data() + j0,
                            static_cast<unsigned long>(len));
                auto c1 = Clock::now();
                gr::uwb::core::uwb_window_power(region.data() + j0, len,
                                                tmpl.size(), winpow.data());
                auto c2 = Clock::now();
                gr::uwb::core::uwb_normalized_score(
                    corr.data(), winpow.data(), len, tenergy, fine_metric.data());
                auto c3 = Clock::now();
                size_t local_best = 0;
                for (size_t k = 0; k < len; ++k)
                    if (fine_metric[k] > fine_metric[local_best])
                        local_best = k;
                auto c4 = Clock::now();
                t_fir += secs(c0, c1);
                t_wpow += secs(c1, c2);
                t_nscore += secs(c2, c3);
                t_argmax += secs(c3, c4);
            }
            auto b1 = Clock::now();
            t_all += secs(b0, b1);
        }

        // Fine only on first peak (early-exit strategy)
        double t_first = 0;
        for (size_t i = 0; i < N; ++i) {
            auto b0 = Clock::now();
            if (!peaks.empty()) {
                size_t p = peaks.front();
                if (p + Lm1 < Rn) {
                    const size_t center = p + Lm1;
                    const size_t j0 = (center > halfw) ? center - halfw : 0;
                    const size_t j1 = std::min(center + halfw, Rn - 1);
                    const size_t len = j1 - j0 + 1;
                    fir.filterN(corr.data(), region.data() + j0,
                                static_cast<unsigned long>(len));
                    gr::uwb::core::uwb_window_power(region.data() + j0, len,
                                                    tmpl.size(), winpow.data());
                    gr::uwb::core::uwb_normalized_score(corr.data(), winpow.data(),
                                                        len, tenergy,
                                                        fine_metric.data());
                }
            }
            auto b1 = Clock::now();
            t_first += secs(b0, b1);
        }

        const double inv = 1.0 / N;
        std::printf("--- C. Fine ROI correlation (per region) ---\n");
        std::printf("  coarse peaks/region : %zu\n", peaks.size());
        std::printf("  ROI half-width      : %zu  (avg len/call ≈ %.0f)\n", halfw,
                    fine_calls > 0
                        ? static_cast<double>(total_len) / fine_calls
                        : 0.0);
        std::printf("  FIR filterN         : %7.3f ms  (%5.1f%% of fine)\n",
                    t_fir * inv * 1e3, 100.0 * t_fir / t_all);
        std::printf("  window power        : %7.3f ms  (%5.1f%% of fine)\n",
                    t_wpow * inv * 1e3, 100.0 * t_wpow / t_all);
        std::printf("  normalized score    : %7.3f ms  (%5.1f%% of fine)\n",
                    t_nscore * inv * 1e3, 100.0 * t_nscore / t_all);
        std::printf("  argmax              : %7.3f ms  (%5.1f%% of fine)\n",
                    t_argmax * inv * 1e3, 100.0 * t_argmax / t_all);
        std::printf("  ALL peaks fine      : %7.3f ms\n", t_all * inv * 1e3);
        std::printf("  FIRST peak only     : %7.3f ms  (%.1fx cheaper)\n\n",
                    t_first * inv * 1e3, t_all / t_first);
    }

    // =====================================================================
    // D. Capture / PDU materialization
    // =====================================================================
    {
        const size_t cap = kUserPreTrigger + kCapture; // 202032
        const size_t lo_off = kRegionPreTrigger;       // approx start
        double t_vec = 0, t_pmt = 0;
        for (size_t i = 0; i < N; ++i) {
            auto a0 = Clock::now();
            std::vector<gr_complex> iq(
                region.begin() + static_cast<std::ptrdiff_t>(lo_off),
                region.begin() +
                    static_cast<std::ptrdiff_t>(lo_off + cap));
            auto a1 = Clock::now();
            pmt::pmt_t data = pmt::init_c32vector(iq.size(), iq);
            pmt::pmt_t meta = pmt::make_dict();
            meta = pmt::dict_add(meta, pmt::mp("packet_id"), pmt::from_long(0));
            meta = pmt::dict_add(meta, pmt::mp("start_sample"),
                                 pmt::from_uint64(0));
            meta = pmt::dict_add(meta, pmt::mp("sample_count"),
                                 pmt::from_long(static_cast<long>(cap)));
            pmt::pmt_t msg = pmt::cons(meta, data);
            (void)msg;
            auto a2 = Clock::now();
            t_vec += secs(a0, a1);
            t_pmt += secs(a1, a2);
        }
        std::printf("--- D. Capture / PDU materialization (cap=%zu) ---\n", cap);
        std::printf("  vector copy IQ      : %7.3f ms  (%.1f GB/s)\n",
                    t_vec / N * 1e3,
                    (cap * 8.0) / (t_vec / N) / 1e9);
        std::printf("  pmt init + meta     : %7.3f ms\n", t_pmt / N * 1e3);
        std::printf("  TOTAL PDU build     : %7.3f ms\n\n",
                    (t_vec + t_pmt) / N * 1e3);
    }

    // =====================================================================
    // E. End-to-end per-region worker path + sparse GR pipeline reference
    // =====================================================================
    {
        double t_worker = 0;
        size_t dets = 0;
        for (size_t i = 0; i < N; ++i) {
            size_t cap = 0;
            auto a0 = Clock::now();
            if (process_region_like_detector(
                    region, tmpl_ds4, tmpl_ds4.size(), fir, tenergy, tmpl.size(),
                    kUserPreTrigger, kCapture, sig_ds, pow_ds, score_ds,
                    metric_ds, coarse_peaks, corr, winpow, fine_metric, cap))
                ++dets;
            auto a1 = Clock::now();
            t_worker += secs(a0, a1);
        }
        std::printf("--- E. End-to-end summary (per region) ---\n");
        std::printf("  worker path total   : %7.3f ms  (%zu/%zu detections)\n",
                    t_worker / N * 1e3, dets, N);
        std::printf("  → region IQ rate    : %.1f MS/s\n",
                    Rn / (t_worker / N) / 1e6);
        std::printf("  → max packet rate   : %.0f pkt/s\n\n",
                    N / t_worker);
    }

    // Re-measure coarse/fine/pdu with PRODUCTION bounds (horizon + first peak)
    {
        double t_c = 0, t_f = 0, t_p = 0;
        const size_t scan_end =
            production_coarse_scan_end(Rn, tmpl.size(), /*candidate_offset=*/0);
        float mx = 0.0f;
        gr::uwb::core::uwb_coarse_peaks(
            region.data(), 0, scan_end, tmpl_ds4.data(), tmpl_ds4.size(), 4, 1,
            tmpl_ds4.size(), 0.5f, 0.5f, 1, sig_ds, pow_ds, score_ds, metric_ds,
            coarse_peaks, &mx);
        const auto peaks = coarse_peaks;
        const size_t Lm1 = tmpl.size() - 1;
        const size_t halfw = kCoarseStride * kCoarseDec + kCoarseMargin;

        for (size_t i = 0; i < N; ++i) {
            auto a0 = Clock::now();
            float m = 0.0f;
            gr::uwb::core::uwb_coarse_peaks(
                region.data(), 0, scan_end, tmpl_ds4.data(), tmpl_ds4.size(), 4,
                1, tmpl_ds4.size(), 0.5f, 0.5f, 1, sig_ds, pow_ds, score_ds,
                metric_ds, coarse_peaks, &m);
            auto a1 = Clock::now();

            // P1 first-peak fine
            for (size_t p : peaks) {
                if (p + Lm1 >= Rn)
                    continue;
                const size_t center = p + Lm1;
                const size_t j0 = (center > halfw) ? center - halfw : 0;
                const size_t j1 = std::min(center + halfw, Rn - 1);
                const size_t len = j1 - j0 + 1;
                fir.filterN(corr.data(), region.data() + j0,
                            static_cast<unsigned long>(len));
                gr::uwb::core::uwb_window_power(region.data() + j0, len,
                                                tmpl.size(), winpow.data());
                gr::uwb::core::uwb_normalized_score(
                    corr.data(), winpow.data(), len, tenergy, fine_metric.data());
                size_t local_best = 0;
                for (size_t k = 1; k < len; ++k)
                    if (fine_metric[k] > fine_metric[local_best])
                        local_best = k;
                if (fine_metric[local_best] >= 0.5f)
                    break;
            }
            auto a2 = Clock::now();

            const size_t cap = kUserPreTrigger + kCapture;
            const size_t lo = kRegionPreTrigger;
            std::vector<gr_complex> iq(
                region.begin() + static_cast<std::ptrdiff_t>(lo),
                region.begin() + static_cast<std::ptrdiff_t>(lo + cap));
            pmt::pmt_t data = pmt::init_c32vector(iq.size(), iq);
            (void)data;
            auto a3 = Clock::now();

            t_c += secs(a0, a1);
            t_f += secs(a1, a2);
            t_p += secs(a2, a3);
        }
        const double tot = t_c + t_f + t_p;
        std::printf("--- F. Worker breakdown PRODUCTION (horizon+first-peak) ---\n");
        std::printf("  coarse_scan_end     : %zu / %zu (%.1f%% of region)\n",
                    scan_end, Rn, 100.0 * scan_end / Rn);
        std::printf("  coarse              : %7.3f ms  %5.1f%%\n",
                    t_c / N * 1e3, 100.0 * t_c / tot);
        std::printf("  fine (first peak)   : %7.3f ms  %5.1f%%\n",
                    t_f / N * 1e3, 100.0 * t_f / tot);
        std::printf("  capture+pmt         : %7.3f ms  %5.1f%%\n",
                    t_p / N * 1e3, 100.0 * t_p / tot);
        std::printf("  TOTAL               : %7.3f ms\n\n", tot / N * 1e3);
    }

    // =====================================================================
    // G. Optimization suggestions (printed from measured ranking)
    // =====================================================================
    std::printf("--- G. Optimization recommendations (data-driven) ---\n");
    std::printf(
        "1. Coarse VOLK score loop is the dominant per-region cost\n"
        "   (O((N/D)*(L/D)) with D=4, L=1016 → ~66k×254 complex MACs).\n"
        "   Highest-ROI options:\n"
        "   a) Scan only preamble-likely prefix of Region (first ~70–90k\n"
        "      samples ≈ SYNC+SFD), not full 264k payload tail — up to ~3×.\n"
        "   b) Raise coarse D 4→8 if detection QA still passes — ~4× on\n"
        "      correlation (D²); verify with qa + real cfile.\n"
        "   c) Early-exit coarse once first strong peak cluster found and\n"
        "      existence check passes (stop scanning rest of region).\n"
        "   d) Do NOT use score stride>1 (already proven to miss peaks).\n"
        "\n"
        "2. Fine correlation is secondary if many coarse peaks; if only\n"
        "   earliest start is needed, fine the first confirmed peak only\n"
        "   (measured multi-peak vs first-peak speedup above).\n"
        "\n"
        "3. Capture+PMT (~202k complex copy) is small vs coarse but shows up\n"
        "   under high packet rates; zero-copy / preallocated PMT or SC16\n"
        "   in PDU would cut memcpy+alloc.\n"
        "\n"
        "4. Energy gate SEARCH is already ~ns/sample (multi-GS/s); not a\n"
        "   target. IN_REGION memcpy is ~hundreds of MS/s — only matters in\n"
        "   dense continuous energy, not at 1 pkt/s.\n"
        "\n"
        "5. Full GR sparse pipeline (~160–210 MS/s input) is limited by\n"
        "   scheduler/buffer plumbing once per-region work is rare; profile\n"
        "   source→null_sink separately before more algorithm work for 1 GS/s\n"
        "   silence-dominated streams.\n"
        "\n"
        "6. Recommended next experiment order:\n"
        "   (P0) limit coarse scan to [0, preamble_horizon)\n"
        "   (P1) early-exit after first SYNC peak cluster\n"
        "   (P1) optional D=8 with correctness gate\n"
        "   (P2) first-peak-only fine; async/zero-copy PDU\n"
        "   (P2) GR plumbing / larger buffers for silence path\n");
    std::printf("\n");
    return 0;
}

// ---------------------------------------------------------------------------
// MixedPattern: superposes radar bursts (code 9) at the predicted radar slots
// and comm bursts (code 10) at a fixed rate, via TRUE complex IQ addition
// (same coordinate law as the schedule core).  Filling a chunk is O(n) with
// bulk memcpy/+= inside each overlapping burst — window count stays schedule-
// only regardless of comm density.
// ---------------------------------------------------------------------------
struct MixedPattern {
    std::vector<gr_complex> radar_pkt;   // code-9 radar preamble burst
    std::vector<gr_complex> comm_pkt;    // code-10 comm burst
    double fs = 998.4e6;
    uint64_t t0 = 0;                       // radar first predicted start
    double radar_period_exact = 0.0;       // T * fs (double, never rounded)
    double comm_period_exact = 0.0;        // fs / comm_rate (0 => no comm)
    double comm_offset = 0.0;              // absolute first comm start
    float comm_amp = 1.0f;                 // comm amplitude scale (SIR)

    int64_t radar_pred(uint64_t k) const
    {
        return static_cast<int64_t>(std::llround(
            static_cast<double>(t0) + static_cast<double>(k) * radar_period_exact));
    }
    int64_t comm_pos(uint64_t m) const
    {
        return static_cast<int64_t>(std::llround(
            comm_offset + static_cast<double>(m) * comm_period_exact));
    }

    void add_burst(gr_complex* out,
                   uint64_t abs0,
                   size_t n,
                   int64_t start,
                   const std::vector<gr_complex>& pkt,
                   float amp) const
    {
        const int64_t rel = start - static_cast<int64_t>(abs0);
        if (rel >= static_cast<int64_t>(n))
            return;
        if (rel + static_cast<int64_t>(pkt.size()) <= 0)
            return;
        const size_t j0 = (rel > 0) ? static_cast<size_t>(rel) : 0;
        const size_t src = (rel > 0) ? 0 : static_cast<size_t>(-rel);
        const size_t len = std::min(pkt.size() - src, n - j0);
        if (amp == 1.0f) {
            for (size_t i = 0; i < len; ++i)
                out[j0 + i] += pkt[src + i];
        } else {
            for (size_t i = 0; i < len; ++i)
                out[j0 + i] += pkt[src + i] * amp;
        }
    }

    // Fill out[0,n) with the stream covering absolute [abs0, abs0+n).
    void fill(uint64_t abs0, gr_complex* out, size_t n) const
    {
        std::memset(out, 0, n * sizeof(gr_complex));
        if (!radar_pkt.empty() && radar_period_exact > 0) {
            const int64_t lo =
                static_cast<int64_t>(abs0) - static_cast<int64_t>(radar_pkt.size());
            const int64_t hi = static_cast<int64_t>(abs0 + n);
            const int64_t k_lo = static_cast<int64_t>(std::floor(
                (static_cast<double>(lo) - static_cast<double>(t0)) /
                radar_period_exact));
            const int64_t k_hi = static_cast<int64_t>(std::ceil(
                (static_cast<double>(hi) - static_cast<double>(t0)) /
                radar_period_exact));
            for (int64_t k = std::max<int64_t>(k_lo, 0); k <= k_hi; ++k) {
                if (k - k_lo > 256)
                    break; // guard against pathological periods
                add_burst(out, abs0, n, radar_pred(static_cast<uint64_t>(k)),
                          radar_pkt, 1.0f);
            }
        }
        if (!comm_pkt.empty() && comm_period_exact > 0 && comm_amp > 0.0f) {
            const int64_t lo =
                static_cast<int64_t>(abs0) - static_cast<int64_t>(comm_pkt.size());
            const int64_t hi = static_cast<int64_t>(abs0 + n);
            const int64_t m_lo = static_cast<int64_t>(std::floor(
                (static_cast<double>(lo) - comm_offset) / comm_period_exact));
            const int64_t m_hi = static_cast<int64_t>(std::ceil(
                (static_cast<double>(hi) - comm_offset) / comm_period_exact));
            for (int64_t m = std::max<int64_t>(m_lo, 0); m <= m_hi; ++m) {
                if (m - m_lo > 512)
                    break;
                add_burst(out, abs0, n, comm_pos(static_cast<uint64_t>(m)),
                          comm_pkt, comm_amp);
            }
        }
    }
};

// ---------------------------------------------------------------------------
// ScheduledMixedSource: GR source that streams MixedPattern (real radar+comm
// superposition) up to `target` samples without storing the whole stream.
// ---------------------------------------------------------------------------
class ScheduledMixedSource : public gr::sync_block
{
public:
    ScheduledMixedSource(const MixedPattern& pat, uint64_t target)
        : gr::sync_block("scheduled_mixed_source",
                         gr::io_signature::make(0, 0, 0),
                         gr::io_signature::make(1, 1, sizeof(gr_complex))),
          d_pat(pat),
          d_target(target)
    {
    }

    int work(int noutput_items,
             gr_vector_const_void_star&,
             gr_vector_void_star& output_items) override
    {
        auto* out = reinterpret_cast<gr_complex*>(output_items[0]);
        const uint64_t remaining = d_target - d_total;
        if (remaining == 0)
            return -1;
        const int n = static_cast<int>(
            std::min<uint64_t>(remaining, static_cast<uint64_t>(noutput_items)));
        d_pat.fill(d_total, out, static_cast<size_t>(n));
        d_total += static_cast<uint64_t>(n);
        d_stats.record(n);
        return n;
    }

    uint64_t total_produced() const { return d_total; }
    const WorkChunkStats& stats() const { return d_stats; }

private:
    MixedPattern d_pat;
    uint64_t d_target;
    uint64_t d_total = 0;
    WorkChunkStats d_stats;
};

// ---------------------------------------------------------------------------
// Scheduled extractor bench: radar-rate windows only; optional dense comm
// bursts (code 10) superposed via real complex IQ addition.  Proves window
// count is schedule-only and reports radar/comm/collision verification.
// ---------------------------------------------------------------------------
int run_scheduled_bench(uint64_t target,
                        double radar_rate_hz,
                        double comm_rate_hz,
                        long buffer_items,
                        int max_noutput_items,
                        int repeat,
                        const std::string& cfile,
                        const std::string& comm_cfile,
                        float comm_amp)
{
    const double fs = 998.4e6;
    if (radar_rate_hz <= 0.0)
        radar_rate_hz = 200.0;
    const double T = 1.0 / radar_rate_hz;
    const size_t pre = static_cast<size_t>(std::llround(10e-6 * fs));
    const size_t capture = static_cast<size_t>(std::llround(190e-6 * fs));
    const size_t post = 4096;
    const uint64_t t0 = pre + 1000; // first predicted start safely inside

    // Load radar preamble burst (code 9) and comm burst (code 10, different
    // preamble) from cfiles.  comm_rate == 0 => no comm superposition.
    std::vector<gr_complex> pkt, tmpl;
    if (!load_packet_and_template(cfile, pkt, tmpl)) {
        std::cerr << "run_scheduled_bench: cannot load radar cfile " << cfile
                  << "\n";
        return 1;
    }
    std::vector<gr_complex> radar_pkt(pkt.begin(), pkt.begin() + kRadarBurstLen);
    std::vector<gr_complex> radar_tmpl(radar_pkt.begin(),
                                       radar_pkt.begin() + kSymbolLen);

    std::vector<gr_complex> comm_pkt, comm_tmpl;
    if (comm_rate_hz > 0.0) {
        std::ifstream f(comm_cfile, std::ios::binary);
        if (!f) {
            std::cerr << "run_scheduled_bench: cannot open comm cfile "
                      << comm_cfile << "\n";
            return 1;
        }
        f.seekg(0, std::ios::end);
        size_t bytes = static_cast<size_t>(f.tellg());
        f.seekg(0, std::ios::beg);
        comm_pkt.resize(bytes / sizeof(gr_complex));
        f.read(reinterpret_cast<char*>(comm_pkt.data()),
               static_cast<std::streamsize>(bytes));
        if (comm_pkt.size() < kSymbolLen) {
            std::cerr << "comm cfile too short\n";
            return 1;
        }
        comm_tmpl.assign(comm_pkt.begin(), comm_pkt.begin() + kSymbolLen);
    }

    MixedPattern pat;
    pat.radar_pkt = std::move(radar_pkt);
    pat.comm_pkt = std::move(comm_pkt);
    pat.fs = fs;
    pat.t0 = t0;
    pat.radar_period_exact = T * fs;
    pat.comm_period_exact = (comm_rate_hz > 0.0) ? fs / comm_rate_hz : 0.0;
    pat.comm_offset = static_cast<double>(t0); // drifting phase -> periodic collisions
    pat.comm_amp = comm_amp;

    // CORE-only path: real radar+comm superposition, schedule throughput
    // (upper bound) plus per-window radar/comm/collision verification.
    {
        gr::uwb::core::ScheduleConfig cfg;
        cfg.sample_rate = fs;
        cfg.packet_interval_s = T;
        cfg.first_packet_sample_exact = static_cast<double>(t0);
        cfg.pre_guard_samples = pre;
        cfg.capture_samples = capture;
        cfg.post_guard_samples = post;
        cfg.pool_size = 8;
        gr::uwb::core::ScheduledWindowCore core(cfg);
        core.set_schedule(cfg.first_packet_sample_exact, T, fs);

        const uint64_t N = target > 0 ? target : 100000000ULL;
        const size_t chunk = 1 << 20;
        std::vector<gr_complex> buf(chunk);
        std::vector<std::vector<gr_complex>> captured; // for post-loop verify

        const auto a0 = std::chrono::steady_clock::now();
        uint64_t done = 0;
        while (done < N) {
            const size_t n =
                static_cast<size_t>(std::min<uint64_t>(chunk, N - done));
            pat.fill(done, buf.data(), n);
            core.process_chunk(buf.data(), n, done);
            while (core.window_ready()) {
                const auto h = core.take_window();
                captured.push_back(core.window(h).samples); // copy out
                core.release_window(h);
            }
            done += n;
        }
        core.flush_eos();
        while (core.window_ready()) {
            const auto h = core.take_window();
            captured.push_back(core.window(h).samples);
            core.release_window(h);
        }
        const auto a1 = std::chrono::steady_clock::now();
        const double sec = std::chrono::duration<double>(a1 - a0).count();
        const auto st = core.stats();

        // Verification pass — deliberately NOT in the timed loop so the
        // schedule-extraction throughput above is not polluted by correlation.
        uint64_t verified = 0, comm_present = 0, collisions = 0;
        for (const auto& w : captured) {
            const size_t L = kSymbolLen;
            const size_t search_len =
                std::min(w.size() > L ? w.size() - L + 1 : 0, pre * 2 + 64);
            float r_met = 0.0f, c_met = 0.0f;
            size_t r_off = 0, c_off = 0;
            const bool r_ok = gr::uwb::core::verify_template_in_window(
                w.data(), w.size(), radar_tmpl.data(), L, 0, search_len, 0.3f,
                &r_met, &r_off);
            const bool c_ok = gr::uwb::core::verify_template_in_window(
                w.data(), w.size(), comm_tmpl.data(), L, 0, search_len, 0.3f,
                &c_met, &c_off);
            if (r_ok)
                ++verified;
            if (c_ok)
                ++comm_present;
            if (r_ok && c_ok)
                ++collisions;
        }
        std::printf("=== scheduled-mixed CORE (no GR, real IQ superposition) ===\n");
        std::printf("target samples    : %llu\n",
                    static_cast<unsigned long long>(N));
        std::printf("radar_rate        : %.1f /s  comm_rate: %.1f /s  comm_amp: %.2f\n",
                    radar_rate_hz, comm_rate_hz, comm_amp);
        std::printf("elapsed           : %.3f s\n", sec);
        std::printf("input throughput  : %.1f MS/s  (%.3f GS/s)\n",
                    N / sec / 1e6, N / sec / 1e9);
        std::printf("scheduled/completed/dropped : %llu / %llu / %llu\n",
                    static_cast<unsigned long long>(st.scheduled_windows),
                    static_cast<unsigned long long>(st.completed_windows),
                    static_cast<unsigned long long>(st.dropped_windows));
        std::printf("radar_verified / comm_present / collision : %llu / %llu / %llu\n",
                    static_cast<unsigned long long>(verified),
                    static_cast<unsigned long long>(comm_present),
                    static_cast<unsigned long long>(collisions));
        std::printf("queue_high_watermark : %llu\n",
                    static_cast<unsigned long long>(st.queue_high_watermark));
        std::printf("\n");
    }

    // GR flowgraph: ScheduledMixedSource (real radar+comm superposition) →
    // UwbScheduledExtractor → every_slot PDU.  Verification stays OFF in the
    // timed GR path: enabling it makes the EOS drain wait on the worker
    // verifying every window (~41M MAC each), which pollutes throughput.  The
    // block-level verification/collision metadata is covered by the CORE pass
    // below and by qa_uwb_scheduled_extractor (collision still emits).
    const bool verify = false;
    std::vector<double> rates;
    for (int r = 0; r < repeat; ++r) {
        auto src = gnuradio::get_initial_sptr(new ScheduledMixedSource(
            pat, target > 0 ? target : 50000000ULL));
        auto ext = gr::uwb::UwbScheduledExtractor::make(
            fs, T, t0, pre, capture, post, 8,
            gr::uwb::UwbScheduledExtractor::EmitPolicy::EverySlot, verify,
            verify ? radar_tmpl : std::vector<gr_complex>(), 0.3f,
            verify ? comm_tmpl : std::vector<gr_complex>(), 0.3f);
        if (max_noutput_items > 0)
            ext->set_max_noutput_items(max_noutput_items);
        else
            ext->set_max_noutput_items(1048576);

        auto tb = gr::make_top_block("bench_scheduled");
        tb->connect(src, 0, ext, 0);
        if (buffer_items > 0) {
            std::shared_ptr<gr::block> srcb = src;
            srcb->set_max_output_buffer(0, -1);
            srcb->set_min_output_buffer(0, buffer_items);
        }

        const auto t0c = std::chrono::steady_clock::now();
        tb->run();
        const auto t1c = std::chrono::steady_clock::now();
        const double sec = std::chrono::duration<double>(t1c - t0c).count();
        const double n = static_cast<double>(src->total_produced());
        rates.push_back(n / sec / 1e6);
        std::printf("=== scheduled GR run %d (real IQ superposition, "
                    "comm_rate %.0f/s) ===\n",
                    r + 1, comm_rate_hz);
        std::printf("processed samples : %.0f\n", n);
        std::printf("elapsed           : %.3f s\n", sec);
        std::printf("input throughput  : %.1f MS/s\n", n / sec / 1e6);
        std::printf("scheduled/completed/emitted/dropped : %llu / %llu / %llu / %llu\n",
                    static_cast<unsigned long long>(ext->scheduled_windows()),
                    static_cast<unsigned long long>(ext->completed_windows()),
                    static_cast<unsigned long long>(ext->emitted_windows()),
                    static_cast<unsigned long long>(ext->dropped_windows()));
        std::printf("verification_failures / collisions : %llu / %llu\n",
                    static_cast<unsigned long long>(ext->verification_failures()),
                    static_cast<unsigned long long>(ext->collisions()));
        std::printf("queue_high_watermark : %llu\n",
                    static_cast<unsigned long long>(ext->queue_high_watermark()));
        std::printf("work mean noutput : %.1f\n", ext->work_mean_noutput_items());
        std::printf("\n");
    }
    if (!rates.empty()) {
        std::sort(rates.begin(), rates.end());
        const double med = rates[rates.size() / 2];
        std::printf("GR scheduled median : %.1f MS/s over %d runs\n", med, repeat);
        std::printf(
            "NOTE: if << 1 GS/s, host GR plumbing (source→sink) is the\n"
            "limit; core-only path above is the schedule-extractor bound.\n");
    }
    return 0;
}

} // namespace

int main(int argc, char** argv)
{
    std::string cfile = (argc > 1) ? argv[1] : "testdata/uwb_code9_preamble64_payload128_standard_sfd.cfile";
    std::string mode = (argc > 2) ? argv[2] : "coarse";
    size_t reps = 4;          // how many times to repeat the signal in memory
    size_t ed = 100;          // energy gate decimation
    size_t cd = 8;            // coarse decimation
    uint64_t target = 500000000ULL; // detector-mode samples to process
    // Default sparse gap ≈ 1 pkt/s for plumbing-primary runs (scheme Phase B).
    size_t det_gap = 998143552;
    bool gap_set = false;
    size_t n_regions = 200;         // detector-region continuous block count
    long buffer_items = 0;
    int max_noutput_items = 0;
    std::string source_type = "pattern";
    float energy_threshold = 1e-3f;
    bool threshold_set = false;
    int repeat = 1;

    for (int i = 3; i + 1 < argc; i += 2) {
        std::string k = argv[i];
        if (k == "--reps") reps = std::stoul(argv[i + 1]);
        else if (k == "--energy-dec") ed = std::stoul(argv[i + 1]);
        else if (k == "--coarse-dec") cd = std::stoul(argv[i + 1]);
        else if (k == "--target") target = std::stoull(argv[i + 1]);
        else if (k == "--gap") {
            det_gap = std::stoul(argv[i + 1]);
            gap_set = true;
        } else if (k == "--regions") n_regions = std::stoul(argv[i + 1]);
        else if (k == "--buffer-items") buffer_items = std::stol(argv[i + 1]);
        else if (k == "--max-noutput-items")
            max_noutput_items = std::stoi(argv[i + 1]);
        else if (k == "--source") source_type = argv[i + 1];
        else if (k == "--threshold") {
            energy_threshold = std::stof(argv[i + 1]);
            threshold_set = true;
        } else if (k == "--repeat") repeat = std::stoi(argv[i + 1]);
    }

    // Layered GR modes: same source/target/gap/buffer surface.
    if (mode == "source-null" || mode == "source-search" || mode == "detector" ||
        mode == "detector-sparse" || mode == "detector-dense") {
        LayeredBenchConfig cfg;
        cfg.mode = (mode == "detector") ? "detector-sparse" : mode;
        cfg.target = target;
        if (mode == "detector-dense")
            cfg.gap = gap_set ? det_gap : 6000;
        else
            cfg.gap = det_gap;
        cfg.buffer_items = buffer_items;
        cfg.max_noutput_items = max_noutput_items;
        cfg.source_type = source_type;
        cfg.energy_threshold = energy_threshold;
        cfg.threshold_override = threshold_set;
        cfg.repeat = repeat > 0 ? repeat : 1;
        return run_layered_bench(cfile, cfg);
    }

    if (mode == "detector-region") {
        return run_region_core_bench(cfile, n_regions);
    }

    if (mode == "detector-region-stream") {
        // Continuous region pressure via min-gap stream (default gap 6000).
        const size_t gap = gap_set ? det_gap : 6000;
        LayeredBenchConfig cfg;
        cfg.mode = "detector-sparse";
        cfg.target = target;
        cfg.gap = gap;
        cfg.buffer_items = buffer_items;
        cfg.max_noutput_items = max_noutput_items;
        cfg.source_type = source_type;
        cfg.energy_threshold = energy_threshold;
        cfg.threshold_override = threshold_set;
        cfg.repeat = repeat > 0 ? repeat : 1;
        return run_layered_bench(cfile, cfg);
    }

    if (mode == "detector-profile") {
        return run_detector_profile(cfile, n_regions);
    }

    if (mode == "detector-gate") {
        // Energy-gate stage only (state machine, no coarse/fine/PDU).
        return run_gate_bench(cfile, target, det_gap);
    }

    if (mode == "scheduled-extractor" || mode == "scheduled-mixed") {
        // Fixed-interval radar-slot path (Phase-1 schedule extractor).
        // Options: --radar-rate R --comm-rate C --comm-amp A
        //          --comm-cfile PATH --target N --buffer-items ...
        double radar_rate = 200.0;
        double comm_rate = (mode == "scheduled-mixed") ? 2000.0 : 0.0;
        std::string comm_cfile =
            "testdata/uwb_code10_preamble16_payload8.cfile";
        float comm_amp = 1.0f; // equal peak: collisions give comm_metric ~0.5
        for (int i = 3; i + 1 < argc; i += 2) {
            std::string k = argv[i];
            if (k == "--radar-rate")
                radar_rate = std::stod(argv[i + 1]);
            else if (k == "--comm-rate")
                comm_rate = std::stod(argv[i + 1]);
            else if (k == "--comm-amp")
                comm_amp = std::stof(argv[i + 1]);
            else if (k == "--comm-cfile")
                comm_cfile = argv[i + 1];
        }
        return run_scheduled_bench(target, radar_rate, comm_rate, buffer_items,
                                   max_noutput_items, repeat > 0 ? repeat : 1,
                                   cfile, comm_cfile, comm_amp);
    }

    if (mode == "demod-core" || mode == "demod-stage-profile" ||
        mode == "demod-pdu" || mode == "scheduled-demod-e2e") {
        // UWB realtime-demodulator benchmark (开发方案_UWB实时解调.md §9).
        auto prof = gr::uwb::demod::Qm35825Profile::Default();
        std::printf("=== UWB realtime demod bench ===\n");
        std::printf("mode                  : %s\n", mode.c_str());
        std::printf("profile               : fs=%.0f code=%zu preamble=%zu "
                    "sfd=%s data_rate=%.2f\n",
                    prof.sample_rate, prof.code_index, prof.preamble_repetitions,
                    prof.sfd_mode, prof.data_rate_mbps);
        std::printf("chips_per_symbol      : %zu\n",
                    gr::uwb::demod::kQm35ChipsPerSymbol);
        std::printf("golden vectors        : testdata/realtime_demod_golden/\n");
        std::printf("implemented stages    : 7/7 (timing/cfo/sfd/cir/"
                    "ns_sfd/phr/payload done)\n");

        if (mode == "demod-stage-profile") {
            // Per-stage latency over the clean golden window (R2).
            const std::string gw = "testdata/realtime_demod_golden/window.cfile";
            std::ifstream f(gw, std::ios::binary);
            if (!f) {
                std::printf("NOTE: %s not found (run from repo root); "
                            "skeleton only.\n", gw.c_str());
                return 0;
            }
            f.seekg(0, std::ios::end);
            const size_t bytes = static_cast<size_t>(f.tellg());
            f.seekg(0, std::ios::beg);
            std::vector<std::complex<float>> rx(bytes / sizeof(std::complex<float>));
            f.read(reinterpret_cast<char*>(rx.data()),
                   static_cast<std::streamsize>(bytes));

            gr::uwb::demod::core::DemodScratch scratch;
            scratch.reserve(rx.size());

            std::vector<std::complex<float>> tmpl;
            std::ifstream tf("testdata/reference_preamble.bin", std::ios::binary);
            tf.seekg(0, std::ios::end);
            tmpl.resize(static_cast<size_t>(tf.tellg()) /
                        sizeof(std::complex<float>));
            tf.seekg(0, std::ios::beg);
            tf.read(reinterpret_cast<char*>(tmpl.data()),
                    static_cast<std::streamsize>(tmpl.size() *
                                                 sizeof(std::complex<float>)));
            // The golden window was generated with the lrwpan generator's
            // default (IEEE legacy) SFD, not the profile's 4z2 — match it.
            const auto sfd_seq = gr::uwb::demod::GetSfdSequence("ieee");
            const int8_t* pc = gr::uwb::demod::GetPreambleCode(prof.code_index);
            std::vector<int8_t> pcode(pc, pc + gr::uwb::demod::kQm35CodeLength);

            gr::uwb::demod::TimingResult tr;
            gr::uwb::demod::CfoResult cf;
            gr::uwb::demod::SfdResult sr;
            gr::uwb::demod::CirResult cir;
            gr::uwb::demod::NsSfdResult ns;
            gr::uwb::demod::PhrResult phr;
            gr::uwb::demod::PayloadResult pay;
            auto chain = [&](bool measure, double* d_t, double* d_c, double* d_s,
                             double* d_r, double* d_n, double* d_p,
                             double* d_pay) {
                bool ok = true;
                auto t_ref = std::chrono::steady_clock::now();
                auto el = [&]() {
                    return std::chrono::duration<double, std::micro>(
                               std::chrono::steady_clock::now() - t_ref)
                        .count();
                };
                ok &= gr::uwb::demod::core::stage_timing(
                    rx.data(), rx.size(), prof, tmpl, 9984, tr, scratch);
                if (measure) {
                    *d_t = el();
                    t_ref = std::chrono::steady_clock::now();
                }
                ok &= gr::uwb::demod::core::stage_cfo(
                    rx.data(), rx.size(), prof, tr, cf, scratch);
                if (measure) {
                    *d_c = el();
                    t_ref = std::chrono::steady_clock::now();
                }
                ok &= gr::uwb::demod::core::stage_sfd(
                    rx.data(), rx.size(), prof, tr, sfd_seq, tmpl, sr, scratch);
                if (measure) {
                    *d_s = el();
                    t_ref = std::chrono::steady_clock::now();
                }
                ok &= gr::uwb::demod::core::stage_cir_softchips(
                    scratch.derotated.data(), rx.size(), prof, tr, pcode, cir,
                    scratch);
                if (measure) {
                    *d_r = el();
                    t_ref = std::chrono::steady_clock::now();
                }
                ok &= gr::uwb::demod::core::stage_ns_sfd(
                    scratch.soft_chips, prof, sfd_seq,
                    gr::uwb::demod::kQm35ChipsPerSymbol, ns, scratch);
                if (measure) {
                    *d_n = el();
                    t_ref = std::chrono::steady_clock::now();
                }
                ok &= gr::uwb::demod::core::stage_phr(
                    scratch.soft_chips, prof, ns,
                    gr::uwb::demod::kQm35ChipsPerSymbol, phr, scratch);
                if (measure) {
                    *d_p = el();
                    t_ref = std::chrono::steady_clock::now();
                }
                ok &= gr::uwb::demod::core::stage_payload_fcs(
                    scratch.soft_chips, prof, phr, ns,
                    gr::uwb::demod::kQm35ChipsPerSymbol, pay, scratch);
                if (measure)
                    *d_pay = el();
                return ok;
            };
            // Warm-up pass (allocations / cold cache), then measured pass.
            chain(false, nullptr, nullptr, nullptr, nullptr, nullptr, nullptr,
                  nullptr);
            double d_timing = 0, d_cfo = 0, d_sfd = 0, d_cir = 0, d_ns = 0,
                   d_phr = 0, d_pay = 0;
            const bool all_ok = chain(true, &d_timing, &d_cfo, &d_sfd, &d_cir,
                                      &d_ns, &d_phr, &d_pay);
            const double d_total = d_timing + d_cfo + d_sfd + d_cir + d_ns +
                                   d_phr + d_pay;

            std::printf("stages                : %s\n",
                        all_ok ? "timing[ok] cfo[ok] sfd[ok] cir[ok] "
                                "ns_sfd[ok] phr[ok] payload[ok]"
                               : "ONE OR MORE STAGES FAILED");
            std::printf("timing start=%lld period=%.3f peaks=%zu metric=%.3f\n",
                        static_cast<long long>(tr.preamble_start_sample),
                        tr.measured_period, tr.detected_peaks, tr.metric);
            std::printf("cfo   hz=%.1f peaks_used=%zu\n", cf.cfo_hz,
                        cf.peaks_used);
            std::printf("sfd   start=%lld metric=%.4f\n",
                        static_cast<long long>(sr.sfd_start_sample), sr.metric);
            std::printf("cir   first_path=%zu peak=%.4f taps=%zu "
                        "soft_chips=%zu spc=%.2f\n",
                        cir.first_path_sample, cir.cir_peak_metric,
                        cir.cir_values.size(), cir.soft_chip_count,
                        cir.samples_per_chip);
            std::printf("ns_sfd start_chip=%lld end_chip=%lld polarity=%d "
                        "metric=%.4f\n",
                        static_cast<long long>(ns.sfd_start_chip),
                        static_cast<long long>(ns.sfd_end_chip), ns.polarity,
                        ns.metric);
            std::printf("phr   psdu=%u rate=%.2f secded_ok=%d\n",
                        phr.psdu_length, phr.data_rate_mbps,
                        phr.secded_uncorrectable ? 0 : 1);
            std::printf("payload bytes=%zu fcs_pass=%d recv=0x%04x "
                        "calc=0x%04x\n",
                        pay.bytes.size(), pay.fcs_pass ? 1 : 0,
                        pay.received_fcs, pay.calculated_fcs);
            std::printf("latency(us)           : timing=%.1f cfo=%.1f "
                        "sfd=%.1f cir=%.1f ns_sfd=%.1f phr=%.1f payload=%.1f "
                        "total=%.1f\n",
                        d_timing, d_cfo, d_sfd, d_cir, d_ns, d_phr, d_pay,
                        d_total);
            std::printf("soft-chip rate        : %.2f Mchips/s\n",
                        cir.soft_chip_count / (d_cir / 1e6) / 1e6);
            std::printf("NOTE: 7/7 demod stages complete (R0-R4).\n");
            return all_ok ? 0 : 1;
        }
        std::printf("NOTE: demod-core / demod-pdu / scheduled-demod-e2e land "
                    "in R5-R6; skeleton only.\n");
        return 0;
    }

    std::ifstream f(cfile, std::ios::binary);
    if (!f) {
        std::cerr << "cannot open " << cfile << "\n";
        return 1;
    }
    f.seekg(0, std::ios::end);
    size_t bytes = static_cast<size_t>(f.tellg());
    f.seekg(0, std::ios::beg);
    std::vector<gr_complex> base(bytes / sizeof(gr_complex));
    f.read(reinterpret_cast<char*>(base.data()), static_cast<std::streamsize>(bytes));

    // In-memory input: repeat the reference signal so the run is long enough
    // for a stable measurement (and scales toward 1e9 samples).
    std::vector<gr_complex> sig;
    sig.reserve(base.size() * reps);
    for (size_t r = 0; r < reps; ++r)
        sig.insert(sig.end(), base.begin(), base.end());

    std::vector<gr_complex> tmpl(base.begin() + kPacketStart,
                                 base.begin() + kPacketStart + kSymbolLen);

    gr::uwb::UwbPreambleDetector::sptr blk;
    if (mode == "reference") {
        blk = gr::uwb::UwbPreambleDetector::make(tmpl, 0.5f);
    } else if (mode == "fast") {
        blk = gr::uwb::UwbPreambleDetector::make(tmpl, 0.5f, 1e-3f, 32, 1);
    } else {
        blk = gr::uwb::UwbPreambleDetector::make(tmpl, 0.5f, 1e-3f, 8, ed, cd, 8, 16);
    }

    auto src = gr::blocks::vector_source_c::make(sig);
    auto ns0 = gr::blocks::null_sink::make(sizeof(float));
    auto ns1 = gr::blocks::null_sink::make(sizeof(unsigned char));
    auto tb = gr::make_top_block("benchmark_detector");
    tb->connect(src, 0, blk, 0);
    tb->connect(blk, 0, ns0, 0);
    tb->connect(blk, 1, ns1, 0);

    const auto t0 = std::chrono::steady_clock::now();
    tb->run();
    const auto t1 = std::chrono::steady_clock::now();
    const double sec = std::chrono::duration<double>(t1 - t0).count();
    const double n = static_cast<double>(sig.size());

    std::printf("processed samples : %zu\n", sig.size());
    std::printf("elapsed time      : %.3f s\n", sec);
    std::printf("throughput        : %.1f MS/s\n", n / sec / 1e6);
    std::printf("throughput        : %.2f GB/s\n", n * 8.0 / sec / 1e9);
    std::printf("input rate equiv  : %.1f GS/s (%.0f MS/s)\n",
                n / sec / 1e9, n / sec / 1e6);
    std::printf("mode              : %s (energy-dec=%zu, coarse-dec=%zu, reps=%zu)\n",
                mode.c_str(), ed, cd, reps);

    return 0;
}
