# SC16 能量门检测器与性能对比

## 数据路径

```text
SC16 @ 998.4 MS/s
→ integer normalized energy gate
→ SC16 pre-trigger ring / candidate Region
→ background worker: candidate SC16 → CF32 scratch once
→ existing coarse/fine correlation
→ slice original SC16 Region
→ PMT s16vector
→ PacketWriter direct write
```

全速 `work()` 不做浮点格式转换、不分配内存。最终输出来自原始 SC16
Region，因此没有 FC32→SC16 回转，也不会引入二次量化。能量计算先提升到
64 位，再归一化到 full-scale 功率：

```text
energy = (I² + Q²) / 32768²
```

## 交付物

- `UwbDetectorSc16`：独立 SC16 sync block，0 stream output，s16vector PDU。
- `uwb_detector_sc16.block.yml`：GRC block。
- `x410_rfnoc_uwb_detector_sc16.grc`：X410/RFNoC SC16 生产图。
- PacketWriter：s16vector 直接写盘，逐 bit 保真。
- `qa_uwb_detector_sc16.cc`：固定 capture、坐标与 bit-exact QA。
- `benchmark_detector_sc16`：同波形、同参数 CF32/SC16 A/B。

## 性能结果

### 默认小 chunk（历史对照）

命令：

```bash
for i in 1 2 3; do
  gr-uwb/build/apps/benchmark_detector_sc16 \
    testdata/uwb_code9_preamble64_payload128_standard_sfd.cfile \
    500000000
done
```

本机每轮均检测 95/95 packets：

| Run | CF32 MS/s | SC16 MS/s | SC16 speedup |
|---:|---:|---:|---:|
| 1 | 177.36 | 254.71 | 1.436× |
| 2 | 158.76 | 228.53 | 1.439× |
| 3 | 169.69 | 252.65 | 1.489× |

中位吞吐约 169.7 → 252.7 MS/s，输入及 candidate Region 字节数减半。
计时包含 GNU Radio source/plumbing、整数门、SC16 Region copy、候选转换、
粗/精相关和 PDU publication；不包含测试文件读取和预先量化。

该结果仍未达到通用 detector 的 998.4 MS/s 实时目标。已知雷达 `t0/T` 时
继续使用 ScheduledExtractor；SC16 energy detector 面向未知 packet 时刻。

### 1M 大 chunk（旧 vector_source 结果，已判定无效）

上述小 chunk 结果主要受 GNU Radio plumbing 影响。修正 benchmark 后同时对
source、head、detector 和 top block 设置 1,048,576 items，并打印 detector
实际 scheduler grant：平均 chunk 约 275k–297k，最大约 1,048,575。

```bash
# 三轮 CF32-first
for i in 1 2 3; do
  gr-uwb/build/apps/benchmark_detector_sc16 <cfile> \
    500000000 1048576 cf32-first
done

# 三轮 SC16-first，排除顺序偏差
for i in 1 2 3; do
  gr-uwb/build/apps/benchmark_detector_sc16 <cfile> \
    500000000 1048576 sc16-first
done
```

| Order | CF32 MS/s | SC16 MS/s | SC16/CF32 |
|---|---:|---:|---:|
| CF32-first #1 | 716.4 | 536.5 | 0.749× |
| CF32-first #2 | 725.7 | 517.1 | 0.713× |
| CF32-first #3 | 730.8 | 534.4 | 0.731× |
| SC16-first #1 | 768.0 | 537.6 | 0.700× |
| SC16-first #2 | 756.8 | 542.4 | 0.717× |
| SC16-first #3 | 755.1 | 558.9 | 0.740× |

六轮合并中位数约：CF32 **743.0 MS/s**，SC16 **537.0 MS/s**，比值
**0.723×**。每轮两者均检测 95/95 packets。

这组数字不能用于格式或算法比较：当时FC32使用`vector_source_c(vlen=1)`，
SC16使用`vector_source_s(vlen=2)`。GNU Radio的重复vector source按
`noutput_items * vlen`逐标量赋值，导致SC16每个复采样循环两次。独立
source→null实测旧SC16源仅约540–554 MS/s，正好钳住完整SC16链路；并非
detector上限。后续结论以统一bulk source结果为准。

### 纯整数能量门（不接 detector）

SC16 gate 热路径已改为原始整数域：每个参与能量统计的采样计算
`I²+Q²`，使用 `uint64_t` 保存 block/window 累加和；归一化门限仅在构造期
按 `32768² × 有效窗口长度` 转换一次。候选 Region 仍保存原始 SC16，gate
阶段不做 SC16→FC32。FC32 gate 保持原浮点实现，用量化后的同一组样本做
严格 A/B。

独立 benchmark 不连接粗相关、细相关、PDU 或 writer：

```bash
gr-uwb/build/apps/benchmark_energy_gate_formats \
  testdata/uwb_code9_preamble64_payload128_standard_sfd.cfile \
  5000000000 1048576 cf32-first
```

5G samples、1M 调用 chunk，正反顺序各三轮：

| 指标 | CF32 gate | SC16 integer gate |
|---|---:|---:|
| 六轮中位输入吞吐 | 2386.0 MS/s | 5993.7 MS/s |
| 输入字节吞吐（中位样本率换算） | 19.09 GB/s | 23.97 GB/s |
| Region 数/轮 | 952 | 952 |
| Region IQ samples/轮 | 251,575,332 | 251,575,332 |

逐轮 SC16/FC32 speedup 中位数 **2.51×**；六轮全部
`region_results_equal=1`。这个结果只代表 state-machine energy gate + Region
维护能力，不代表后续 coarse/fine detector 或完整 decode 吞吐。

### SC16/Q15粗相关 + FC32细相关ROI

完整SC16 detector进一步改为：

```text
SC16整数能量门
→ SC16候选Region
→ D=4的SC16/Q15归一化粗相关（AVX2 madd + int64累加）
→ 仅粗峰附近约 ROI+1015 samples 转FC32
→ 原FC32细相关定位
→ 从原SC16 Region截包
```

本机VOLK的`16ic`点积只返回16位，无法安全累加1016抽头，因此实现使用
AVX2 `madd_epi16`并立即扩展到64位；无AVX2时使用标量`int64`回退。Q15模板
及虚部乘法模板在构造期生成，worker热路径不为点积分配内存。R=1默认路径
直接比较归一化相关功率，避免逐位置开平方；细相关及最终0.5确认保持FC32。

正确性：合成重复preamble QA中，Q15与FC32粗相关的峰数量及每个峰位置完全
一致；完整真实波形每轮均为95/95 packets。D=8也为95/95，但未显示稳定
优势，故默认保持D=4。

benchmark现使用同一模板化`BulkRepeatSource<complex<T>>`，两种格式都以
一个复采样为一个GNU Radio item，并用分段`memcpy`处理循环回绕。500M
samples、1M buffer/chunk、正反顺序各三轮：

| 指标 | FC32 | SC16 Q15粗相关/FC32细ROI |
|---|---:|---:|
| source-only中位吞吐 | 1311.0 MS/s | 2579.1 MS/s |
| 完整detector中位吞吐 | 913.6 MS/s | 1550.8 MS/s |
| SC16/FC32逐轮比值中位数 | — | 1.700× |
| packets/轮 | 95 | 95 |

隔离版本“SC16整数门→整Region转FC32→FC32粗/细相关”使用同一bulk source
六轮中位 **1530.2 MS/s**，同样95/95。当前Q15/ROI版本快约1.3%，差距较小；
两种SC16后端均超过998.4 MS/s目标。主要改进来自消除benchmark的
`vector_source_s(vlen=2)`假瓶颈，不能把1.55 GS/s全部归因于Q15相关。

## X410 GRC

`x410_rfnoc_uwb_detector_sc16.grc` 使用：

```text
Radio SC16 737.28M
→ RFNoC Upsampler 65/48 SC16
→ RFNoC RxStreamer host output SC16 998.4M
→ UwbDetectorSc16
→ PacketWriter
```

FPGA block type/instance 和幅度 scale 必须在实机上确认。当前默认假设 RFNoC
SC16 的 full scale 为 32768；若自定义 block 改变幅度，应相应调整 energy
threshold 或在 FPGA/metadata 中明确 scale。
