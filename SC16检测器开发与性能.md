# SC16 能量门检测器与性能对比

## 数据路径

```text
SC16 @ 998.4 MS/s
→ integer normalized energy gate
→ SC16 pre-trigger ring / candidate Region
→ background worker: candidate SC16 → CF32 scratch once
→ existing coarse/fine correlation
→ slice original SC16 Region
→ PMT s16vector
→ PacketWriter direct write
```

全速 `work()` 不做浮点格式转换、不分配内存。最终输出来自原始 SC16
Region，因此没有 FC32→SC16 回转，也不会引入二次量化。能量计算先提升到
64 位，再归一化到 full-scale 功率：

```text
energy = (I² + Q²) / 32768²
```

## 交付物

- `UwbDetectorSc16`：独立 SC16 sync block，0 stream output，s16vector PDU。
- `uwb_detector_sc16.block.yml`：GRC block。
- `x410_rfnoc_uwb_detector_sc16.grc`：X410/RFNoC SC16 生产图。
- PacketWriter：s16vector 直接写盘，逐 bit 保真。
- `qa_uwb_detector_sc16.cc`：固定 capture、坐标与 bit-exact QA。
- `benchmark_detector_sc16`：同波形、同参数 CF32/SC16 A/B。

## 性能结果

命令：

```bash
for i in 1 2 3; do
  gr-uwb/build/apps/benchmark_detector_sc16 \
    testdata/uwb_code9_preamble64_payload128_standard_sfd.cfile \
    500000000
done
```

本机每轮均检测 95/95 packets：

| Run | CF32 MS/s | SC16 MS/s | SC16 speedup |
|---:|---:|---:|---:|
| 1 | 177.36 | 254.71 | 1.436× |
| 2 | 158.76 | 228.53 | 1.439× |
| 3 | 169.69 | 252.65 | 1.489× |

中位吞吐约 169.7 → 252.7 MS/s，输入及 candidate Region 字节数减半。
计时包含 GNU Radio source/plumbing、整数门、SC16 Region copy、候选转换、
粗/精相关和 PDU publication；不包含测试文件读取和预先量化。

该结果仍未达到通用 detector 的 998.4 MS/s 实时目标。已知雷达 `t0/T` 时
继续使用 ScheduledExtractor；SC16 energy detector 面向未知 packet 时刻。

## X410 GRC

`x410_rfnoc_uwb_detector_sc16.grc` 使用：

```text
Radio SC16 737.28M
→ RFNoC Upsampler 65/48 SC16
→ RFNoC RxStreamer host output SC16 998.4M
→ UwbDetectorSc16
→ PacketWriter
```

FPGA block type/instance 和幅度 scale 必须在实机上确认。当前默认假设 RFNoC
SC16 的 full scale 为 32768；若自定义 block 改变幅度，应相应调整 energy
threshold 或在 FPGA/metadata 中明确 scale。
